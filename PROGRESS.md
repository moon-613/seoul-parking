# 프로젝트 진행 일지

생활인구·거주형태 기반 서울 행정동별 공영주차 수급 진단 및 확충 우선순위 도출

기간: 2026-08-10 (월) ~ **2026-08-14 (목)** · 4일 프로젝트

---

# 📖 용어 — 질문받으면 이대로 답하면 된다

**데이터**

| 용어 | 뜻 |
| ------ | ---- |
| **생활인구** | 그 시각 그 동네에 **있는** 모든 사람. 사는 사람 + 온 사람. KT 통신데이터 추계 |
| **상주인구** | 그 동네에 **사는** 사람 (주민등록) |
| **직장인구** | 그 동네로 **일하러 오는** 사람 |
| **유동수요** | 방문객까지 포함한 주차 수요 = **생활인구**로 잼 |
| **정주수요** | 공영주차에 의존할 수밖에 없는 수요 = **비아파트 가구**로 잼 |
| **비아파트 가구** | 단독·연립·다세대 가구. **부설주차장이 없어** 공영주차를 써야 하는 집 |
| **패널** | 행정동 424 × 요일 7 × 시간대 5 = **14,840행** 표. 한 줄 = 한 동네의 한 시점 |

**지표**

| 용어 | 뜻 |
| ------ | ---- |
| **천명당 주차면** | 공영주차면 ÷ 생활인구 × 1,000. **"1,000명이 몇 개를 나눠 쓰는가"**. 주차면만 보면 동네 크기에 속으므로 사람 수로 나눈다 |
| **1인당 공영주차면** | 위와 같은 뜻. 1인당은 0.008처럼 소수라 읽기 불편해 ×1,000해서 쓴다 |
| **배정률** | 주차장에 **소속 행정동을 정해준 비율**. 원본에 행정동 정보가 없어 주소로 찾아 붙였다. 856/856 = 100% |
| **조인율** | 서로 다른 데이터를 **같은 동네끼리 이어붙인 비율**. 코드 체계가 달라 7.5%였던 것을 100%로 올렸다 |
| **주거지수** | 상주 ÷ (상주 + 직장). 1에 가까우면 주거지, 0에 가까우면 업무지구 |
| **유입배수** | 생활인구 ÷ 상주인구. 클수록 외부에서 많이 온다 |

**분석 방법**

| 용어 | 뜻 |
| ------ | ---- |
| **회귀선** | "사람이 이만큼 오는 동네면 주차장이 **대략 이 정도는 있겠다**"는 기준선 |
| **회귀 잔차** | **실제 − 기준선**. 음수면 기대보다 부족, 양수면 여유. 규모가 다른 동네를 공정하게 비교하려고 쓴다 |
| **표준화 잔차** | 잔차를 "평균에서 몇 칸 떨어졌나"로 바꾼 값. **−1 이하면 공급부족**, +1 이상이면 공급여유로 판정 |
| **R²** | 설명력. **0~1이고 1에 가까울수록 잘 설명**한다. 우리는 0.068 — 거의 설명이 안 된다 |
| **상관계수 r** | −1~+1. 같이 커지면 +, 반대로 가면 −. **0.3 미만이면 약한 관계** |
| **로그 변환** | 큰 값을 눌러 격차를 줄이는 것. 안 하면 큰 동네가 결과를 독식한다 |
| **PCA (주성분분석)** | 여러 변수를 **몇 개 축으로 요약**. 가중치를 사람이 정하지 않아 임의성이 없다 |
| **군집 (KMeans)** | 비슷한 동네끼리 **자동으로 묶는 것**. 몇 개로 묶을지는 실루엣 계수로 정함 |
| **실루엣 계수** | 군집이 잘 나뉘었는지 점수. 우리는 이 값으로 **2개가 최적**이라고 판단 |
| **프리드먼 검정** | 같은 대상을 여러 시점에서 볼 때 쓰는 검정. **"시간대에 따라 다른가"** 를 물었다 |
| **EDA** | 탐색적 데이터 분석. **가설을 검정하기 전에** 분포·결측·이상치를 훑어보는 단계 |

**⚠️ 헷갈리기 쉬운 것 하나**

> **총량은 늘지만 1인당은 준다.**
> 생활인구 ↔ 공영주차면(총량) = **+0.37** — 사람 많은 동네에 주차장이 더 있는 건 맞다
> 생활인구 ↔ 천명당 주차면(1인당) = **−0.14** — 그런데 사람 수로 나누면 오히려 적다
> 인구 10배일 때 주차면은 3.94배뿐이라, 1인당으로는 **61% 감소**한다.
> **"주차장이 없다"가 아니라 "사람 느는 속도를 못 따라간다"** 가 정확한 표현이다.

---

# 🔜 이어서 할 일 — 2026-08-11 17:00 기준

> **다음 작업: 발표자료(=보고서) PPT 만들기.** 아래만 읽으면 바로 이어갈 수 있다.

## 지금 어디까지 왔나

```
① 기획  →  ② 수집  →  ③ 전처리  →  ④ 분석  →  ⑤ 보고서  →  ⑥ 대시보드  →  ⑦ 발표
  ✅        ✅         ✅          ✅         🟡초안       ✅           ⬜ ← 여기
```

- `reports/report.md` **375줄 초안 완성**. 인용 수치 16개를 산출물과 대조해 전부 일치 확인
- 대시보드 5페이지 동작 중 (`streamlit run dashboard/app.py` → localhost:8501)

## ⚠️ 가장 먼저 알아야 할 것

**보고서와 발표자료는 별개가 아니라 하나다.** 
→ `reports/report.md`는 **PPT의 원고**로 쓰고, 최종 제출물은 PPT다.

## 예시 PPT를 분석한 결과 (이미 확인함)

```
규격    10 × 7.5 inch (4:3) · 25장
구조    I. 분석 배경 및 목표 → II. 활용데이터 → III. 분석 → IV. 결론 → 참고자료
패턴    슬라이드마다 [질문형 제목] + [그래프] + [발견 문장 2~3개]
        예: "총지출은 어떻게 변해왔을까?" → 그래프 → "2018년에 7.1%로 크게 증가…"
        우상단에 섹션 번호(01, 02…)와 섹션명을 반복 표기
```

## 계획해 둔 PPT 구성


| #  | 섹션 | 내용                                                     | 쓸 그림       | 시간 |
| ---- | ------ | ---------------------------------------------------------- | --------------- | ------ |
| 1  |      | 표지                                                     |               |      |
| 2  |      | 목차                                                     |               |      |
| 3  | I    | 분석 배경 — 기존 서비스의 공백                          |               |      |
| 4  | I    | 분석**주제, 목적, 핵심질문** — 두 사용자(주/부)         |               | 1분  |
| 5  | I    | 활용데이터 목록 7종, 주요 이슈와 해결 방법               |               | 2분  |
| 7  | I    | **분석 과정**                                            |               | 4분  |
|    |      | 핵심, 결과, 인사이트                                     |               |      |
| 8  | I    | 분석 도구                                                |               |      |
| 9  | II   | 데이터 결합 — 7.5%→100%, 지오코딩 100%, 총조사 424/424 |               |      |
| 10 | II   | 전처리 — 시간대 5구간 근거 + 공휴일 제외                | `daily_trend` |      |
| 11 | III  | 나들이 적합도 지수 (PCA)                                 | `suitability` |      |
| 12 | III  | 가설                                                     | `real_demand` |      |
| 13 | III  | 확충 우선순위 + 자치구                                   | `district`    |      |
| 14 | IV   | 결론 — 나들이객, 주차 정책 담당                        |               |      |
| 15 | IV   | 대시보드 시연                                            | 동영상        | 2분  |
| 16 |      | 한계와 향후 과제, 배운 점                                |               | 1분  |

모든 코드와 그래프 설명하지 말고

- 가장 어려웠던 문제
- 가장 의미 있던 분석 결과
- 프로젝트를 통해 가장 크게 배운 점

미사용 그림: `residual` `weekday_timeslot` `gu_distribution` (내용이 다른 슬라이드와 겹침)

## 만들 방법

`python-pptx`로 **생성기 스크립트**를 짜는 것으로 정했다 (`src/report/build_deck.py` 예정).
수치가 바뀌면 다시 돌리면 되고, 결과물은 편집 가능한 `.pptx`라 PowerPoint에서 PDF로 내보낼 수 있다.

- ✅ `python-pptx 1.0.2` · `python-docx 1.2.0` · `pillow 12.3.0` **이 맥에 설치 완료**
  (단 `requirements-dev.txt`에는 아직 안 넣음 — 넣어야 Windows에서도 재현됨)
- ⬜  4:3(예시와 동일) 유지
- ⬜ 한글 폰트는 PPT에 이름만 지정되고 실제 렌더링은 여는 쪽 PowerPoint가 한다.
  Windows 기준 `맑은 고딕`으로 지정할 것

## 그 외 남은 제출물


| 항목                  | 위치                        | 상태                    |
| ----------------------- | ----------------------------- | ------------------------- |
| 발표자료(=보고서) PPT | `submission/02_발표자료/`   | ⬜**다음 작업**         |
| 대시보드 스크린샷     | `submission/04_스크린샷/`   | ⬜ 24번 슬라이드에 필요 |
| 최종 데이터           | `submission/03_최종데이터/` | ⬜`panel.csv` 복사      |
| 기획서                | `submission/01_보고서/`     | ✅ 2차 개정 완료        |

## 작업 재개 시 주의

1. **Windows 머신은 오늘 11:19 이후로 pull이 안 됐다.** 그 뒤 14커밋이 이 맥에서 올라갔다.
   Windows에서 이어가려면 `git pull` 먼저. 그림 12종은 `.gitignore`라 분석 스크립트를 다시 돌려야 한다
2. `dong_residual.csv`는 재실행하면 부동소수점 마지막 자릿수 때문에 dirty해진다 → `git checkout`
3. 분석 실행 순서: `residual` → `recommend`, `real_demand`·`suitability` → `district`·`maps`
4. 대시보드를 껐다 켜려면 `pkill -f "streamlit run dashboard"` 후 재실행

## 발표에서 반드시 말할 것 (보고서에 근거 있음)

- **가설 기각 자체가 발견이다** — R² 0.068 / 0.004. 배치가 수요 기반이 아니다
- **잔차를 '수요 대비 부족'으로 읽으면 안 된다** — 순위상관 0.997. 그래서 직접 지표로 순위를 냈다
- **아파트 오탐 4개** — 상계9동은 비아파트 4가구인데 확충 대상으로 잡혔다
- **k=2** — 기획서가 예상한 3유형은 데이터가 지지하지 않는다

---

## 단계별 진행 현황

```
① 기획  →  ② 수집  →  ③ 전처리  →  ④ 분석  →  ⑤ 보고서  →  ⑥ 대시보드  →  ⑦ 발표
  ✅        ✅         ✅          ✅         ⬜          ✅           ⬜
```


| 단계                                          | 상태 | 산출물                                        | 핵심 결과                                                                      |
| ----------------------------------------------- | ------ | ----------------------------------------------- | -------------------------------------------------------------------------------- |
| **① 기획**                                   | ✅   | `submission/01_보고서/기획서.docx`            | 주(나들이객)+부(주차 정책 담당) 사용자.**                                      |
| Day 2에 2회 개정** — 사용자 단일화 후 되돌림 |      |                                               |                                                                                |
| **② 수집**                                   | ✅   | `data/raw`, `data/external`                   | **7종** 확보 (생활인구 56일·주차장 2종·상권 4개·경계·코드표·**총조사**)   |
| **③ 전처리**                                 | ✅   | `data/processed/panel.csv`                    | **14,840행 × 31열**. 코드 조인 7.5%→100%, 주차장 배정 100%, 총조사 매칭 100% |
| **④ 분석**                                   | ✅   | `reports/figures/*.png` (**12종**) · CSV 8종 | **가설 6개 전부 검증 완료** (지지 4 · 약한 지지 1 · 기각 1)                  |
| **⑤ 보고서**                                 | 🟡   | `reports/report.md`                           | **초안 375줄 완료** (수치 16개 검증). 최종 제출물은 PPT                        |
| **⑥ 대시보드**                               | ✅   | `dashboard/` **5페이지**                      | 두 사용자로 분리, 분석 산출물 전부 반영                                        |
| **⑦ 발표**                                   | ⬜   | `submission/02_발표자료`                      | 미착수                                                                         |

**단계별 소요와 병목**

- 가장 오래 걸린 건 **③ 전처리**였습니다. 데이터가 안 붙는 문제(코드 체계 불일치, 주차장에 행정동 없음)를 푸는 데 대부분의 시간이 들었습니다.
- **② 수집**은 API 장애·컬럼 폐지·좌표계 오류 등으로 여러 번 되돌아갔습니다.
- **④ 분석**은 데이터가 정리된 뒤라 빠르게 끝났습니다.

**중간에 바꾼 방향 2가지**


| 시점       | 변경                                       | 이유                                                                                                                      |
| ------------ | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 수집 중    | 실시간 주차 → 구조적 과부족(회귀 잔차)    | `CUR_PARKING` 컬럼 폐지 확인                                                                                              |
| 분석 중    | 주차 공급 →**공영주차 접근성**            | 민영 미공개로 측정 대상을 정직하게 한정                                                                                   |
| Day 2 오전 | 부사용자(행정) 제거 → 주 사용자 단일화    | 확충 우선순위까지 파고들며 초점이 흐려짐                                                                                  |
| Day 2 오후 | **부사용자 복원 + 거주형태 도입** (되돌림) | 단일화한 기획이 분석이 아니라 서비스 기획이 됐고, 핵심 기능(관심 장소 매칭)은 네이버·카카오 API 미제공으로 구현 불가였음 |
| Day 2 오후 | 시간대 4구간(06-24) →**5구간(10-24)**     | 06~10시가 '아침' 평균에 섞여 "일요일 아침이 가장 여유"라는 실행 불가능한 추천이 나오고 있었음                             |

---

# 📚 파일별 상세 — 설명용 레퍼런스

> 각 항목은 **하는 일 → 왜 그렇게 했나 → 핵심 수치 → 나올 질문** 순서.
> 2026-08-11 기준. 수치는 시간대 5구간 + 거주형태 반영 후 값.

## 전체 흐름 한 장

```
[수집]        [전처리]                      [분석]                    [산출]
raw/       →  dong_code.py      ┐
external/     geocode_parking.py │
              load_commercial.py ├→ build_panel.py → panel.csv → 10개 분석 → 그림 12
              load_census.py     ┘        ↓                                  CSV 8
                                     validate.py
```

핵심은 **`panel.csv` 하나로 모든 분석이 돌아간다**는 것.
14,840행 × 31열 = 행정동 424 × 요일 7 × 시간대 5.

---

## 1. 데이터 — 무엇을 왜 썼나


| 데이터                   | 출처                             | 규모          | 이 데이터가 답하는 것                                 |
| -------------------------- | ---------------------------------- | --------------- | ------------------------------------------------------- |
| **생활인구**             | 서울 열린데이터광장 API          | 56일 · 157MB | 언제 사람이 몰리나 (유동수요)                         |
| **주차장 표준데이터**    | 공공데이터포털 API               | 856건         | 공영주차 공급 (주 데이터)                             |
| **서울시 공영주차장**    | 서울 열린데이터광장 API          | 2,189건       | 표준데이터가 0면인 동 보충                            |
| **상권분석 4종**         | 서울 열린데이터광장 (수동)       | 분기          | 매력도(음식점·카페·집객시설), 동네 성격(상주·직장) |
| **거처종류별 가구**      | KOSIS 인구주택총조사 2025 (수동) | 427동         | 정주수요 (부설주차장 없는 가구)                       |
| **행정동 경계**          | SGIS API                         | 426 폴리곤    | 지도 시각화                                           |
| **전국 행정동 코드정보** | 서울 열린데이터광장 (수동)       | 43개월        | 코드 체계가 다른 데이터를 잇는 다리                   |

**왜 주차장이 2종인가** — 서울시 API에는 거주자우선주차구획(1면짜리)이 다수 섞여
방문객 주차 분석에 부적합하다. 서교동이 서울시 API 6면 vs 표준데이터 152면.
그래서 표준데이터를 기본으로 쓰고, **표준데이터에서 0면인 동에만** 서울시 API로 보충했다.
겹치는 동이 없어 이중 계산은 없다. → 73,796면 → **78,267면**, 0면 동 109 → **66개**

**왜 민영주차장이 없나** — 표준데이터 수집 범위가 "지자체 관리 대상"으로 한정되어
서울 25개 자치구 중 구로·금천 2개 구 45건뿐이었다. 그래서 측정 대상을
"전체 주차 공급"이 아니라 **"공영주차 공급"** 으로 정직하게 한정했다.

---

## 2. 전처리 — `src/preprocess/` 6개 파일

### `dong_code.py` (227줄) — 코드 체계를 잇는 다리

**하는 일** 서로 다른 행정동 코드 체계를 하나로 통일한다.

```
생활인구·상권 = 행정안전부 코드(11110530)
SGIS 경계     = SGIS 자체 코드(11010530)
직접 조인하면 424개 중 32개만 일치 (7.5%)
```

**왜 이 방법인가** '전국 행정동 코드정보'를 다리로 삼았다.
`생활인구코드 → ADMI_CD → FULL_NM → SGIS adm_nm → 경계` 경로로 이름을 거쳐 연결한다.

**처리한 예외 4종**

- 가운뎃점 표기 차이 (`상계3.4동` ↔ `상계3·4동`) → `normalize_dong_name()`
- 구버전 코드 7개 → `resolve_legacy_codes()` 가 순서로 매칭
- 동 분할 1:2 (상일동) → `SPLIT_DONGS`
- 월별 누락 → 43개월치를 전부 합쳐서 씀 (통폐합으로 특정 월에만 있는 코드가 있음)

**핵심 수치** 조인율 **7.5% → 100%**, 인구 손실 0명

> **Q. 왜 43개월치를 다 받았나요?**
> 용신동·일원2동처럼 통폐합된 동은 특정 월 코드표에만 존재합니다. 한 달치만 쓰면 누락됩니다.

### `geocode_parking.py` (265줄) — 주차장에 행정동 붙이기

**하는 일** 주차장 데이터에 행정동코드가 없어서 **주소를 행정동코드로 변환**한다.

**왜 공간조인이 아닌가** 위경도가 결측이다(표준데이터 5.7%, 서울시 API 33.5%).
SGIS `addr/geocodewgs84`는 좌표와 함께 **행정동코드를 직접 반환**해서 공간조인이 필요 없다.

**왜 카카오가 아닌가** 앱 서비스 미활성으로 403. 문서에는 "활성화 불필요"라고 적혀 있으나 실제와 다르다.

**처리한 주소 특이 표기**

```
신사동산55번지5호      번지/호 표기
상계6·7동770-2         행정동명 + 지번이 붙음
산10-84                산 지번
A+B+C                  복수 주소
```

주소 후보를 지번 → 군더더기 제거 → 동 단위 순으로 폴백한다.

**핵심 수치** 배정률 **100%** (856/856건, 73,796면), 경계코드와 100% 일치

### `load_commercial.py` (73줄) — 상권 CSV 4종 로드

**하는 일** 수동 다운로드한 점포·집객시설·상주인구·직장인구 CSV를 읽는다.
인코딩을 자동 판별한다(EUC-KR / UTF-8 혼재).

**주의** `-행정동`(OA-22xxx) 버전을 받아야 한다. `-상권`(OA-15xxx)은 공간 단위가 달라 못 쓴다.
병합은 이 파일이 아니라 `build_panel.py`가 한다.

### `load_census.py` (156줄) — 거주형태 결합 ★ Day 2 신설

**하는 일** KOSIS 거처종류별 가구를 패널에 붙이고 **비아파트 가구**를 산출한다.

**왜 필요한가** 생활인구만으로 수요를 재면 **부설주차장이 있는 아파트 거주자까지** 수요로 센다.
공영주차 실수요는 부설주차장이 없는 가구(단독·연립·다세대)에서 나온다.

**왜 합산이 아니라 차감인가** — 가장 자주 나올 질문

```
KOSIS는 가구 수가 적은 소분류를 비공개(X)로 표기
  연립 12.1% · 다세대 7.0% · 단독 6.1% 가 X  → 더하면 결측이 번짐
  일반가구 0건 · 아파트 2건만 X              → 차감하면 결측 11개 → 2개
비아파트 가구 = 일반가구 − 아파트   (두 방식 차이는 최대 5가구)
```

**행정동 매칭이 까다로웠던 이유** 원본에 **자치구 컬럼이 없고 동 이름만** 있다.

```
① 동명이인   신사동(관악·강남) → KOSIS·패널 둘 다 행정동코드 순이라 파일 순서로 구분
              (앞이 관악=서림동 다음, 뒤가 강남=내곡동 다음)
② 통합 동    용신동 = 용두동+신설동 / 상일1동 외 1개 통합 = 상일1동+상일2동
③ 잉여 동    항동(구로구) — 생활인구 미제공이라 제외
427행 − 2 − 2 + 2 − 1 = 424  →  100% 매칭
```

**핵심 수치** 아파트 거주 비율 중앙값 **43.1%** (최저 0.6% 능동 ~ 최고 100% 잠실2동)

> **Q. 아파트면 주차 문제가 없나요?**
> 아닙니다. 1990년대 이전 아파트는 세대당 주차대수가 0.3~0.7대로 지금 기준에 못 미칩니다.
> 그래서 비아파트 가구를 **실수요의 상한이 아니라 대리변수**로 해석한다고 한계에 명시했습니다.

### `build_panel.py` (270줄) — 최종 테이블 조립 ★ 파이프라인의 심장

**하는 일** 위 4개 모듈의 결과를 합쳐 `panel.csv`를 만든다.

**조인 구조**

```
생활인구 ADSTRD_CODE_SE ─┐
상권 행정동_코드          ─┼─ 행안부 행정동코드(admi_cd) 기준
주차장 geo_adm_cd ── 크로스워크 ─┘
총조사 ── 행정동명 + 순서 매칭 ─┘
```

**여기서 하는 판단 3가지**

1. **공휴일 제외** — 평일에 낀 공휴일 2일(6/3 지방선거, 7/17 제헌절)을 요일 평균에서 뺀다.
   두 날 모두 평일 대비 −6.6%로 주말(−6.2%) 수준이었다.
   → 변동계수 수 2.01%→0.19%, 금 2.97%→0.38%
2. **0면은 결측이 아니다** — 주차장 없는 동은 `parking_slots = 0`, `has_parking = False`
3. **시간에 반응하는 파생변수** — 주차·상권·가구는 정적이고 생활인구만 변한다.
   따라서 `slots_per_1k`(= 주차면 ÷ 생활인구)만 요일·시간대에 따라 움직인다.

**산출** `panel.csv` **14,840행 × 31열**

### `validate.py` (216줄) — 자동 품질검증

**하는 일** 5종 검사. 구조 / 중복 / 논리 / 결측률 / 이상치.

**설계 판단** 이상치는 **탐지만 하고 제거하지 않는다.** 명동·강남의 극단값은 오류가 아니라
실제 현상이기 때문이다. 로그로 알리고 판단은 분석자에게 맡긴다.

**Day 2에 고친 것** 기대 행수가 `동수 × 7 × 4`로 박혀 있어 5구간 전환 시 실패했다.
config에서 요일·시간대 개수를 읽도록 바꿨다.

---

## 3. 분석 — `src/analysis/` 10개 파일

> 실행 순서 의존: **`residual` → `recommend`**, **`real_demand`·`suitability` → `district`·`maps`**
> `daily_trend`만 `data/raw`(158MB)가 필요하고 나머지는 `panel.csv`만 있으면 된다.

### `explore.py` (191줄) — 기초 통계 · 가설 1·3

**산출** `corr_heatmap.png` · `weekday_timeslot.png` · `gu_distribution.png`

**가설 1** 유동인구↑ → 1인당 공영주차면↓ → **지지하나 매우 약함 (r = −0.14)**
**가설 3** 20~30대·음식점 밀도 ↔ 주차면 음의 상관 → **기각 (r = −0.02)**

**같이 출력하는 민감도 표** — 0면 66개 동을 넣고 뺀 비교

```
                  제외(현재)        포함
생활인구          r=−0.138(358)   r=−0.102(424)
음식점수          r=−0.022(357)   r=+0.027(423)  ← 부호 반전
```

> **Q. 왜 0면 동을 뺐나요?**
> 민영 미개방 영향이 섞여 있어서입니다. 다만 `slots_per_1k = 0`은 결측이 아니라 실제 값이라
> 포함한 결과도 함께 제시합니다. 결론(가설1 약함·가설3 기각)은 양쪽 동일합니다.

### `daily_trend.py` (170줄) — 일별 추세 · 공휴일 검증

**하는 일** 56일을 요일×시간대로 평균내기 전에 **날짜 축을 직접 본다.**

**왜 필요했나** 처음엔 날짜를 한 번도 안 보고 바로 평균냈다. 출근시간(08~09시) 생활인구로
이상일을 탐지하니 평일 2일이 걸렸다.

**핵심 수치** 변동계수 개선 — 수 2.01%→0.19%, 금 2.97%→0.38%. 6월→7월 추세 변화는 −0.5%로 무시 가능.

### `residual.py` (197줄) — 회귀 잔차 ★ 모든 진단의 기준

**하는 일** "사람이 이만큼 오는 동네면 공영주차가 대략 이 정도는 있어야 한다"는
기준선을 회귀로 긋고, 실제가 그 선보다 얼마나 위/아래인지(잔차)를 본다.

**왜 로그-로그인가** — 자주 나올 질문

```
원단위:   잔차 왜도 +2.36 / 정규성 위배 / 등분산 위배 → 큰 동네가 하위권 독식
로그-로그: 등분산 회복(p=0.45) / 왜도 −0.68 / 해석이 "기대 대비 몇 배"가 됨
```

**핵심 수치** R² = **0.074** (p=1.86e-07) · 공급부족 **56개** / 공급여유 **54개** / 보통 248개
다중회귀로 5변수까지 늘려도 R²=0.092(adj 0.079), VIF 모두 10 미만.

> **Q. R²가 낮은데 의미가 있나요?**
> **설명력이 낮다는 것 자체가 발견입니다.** 서울 공영주차장 공급이 생활인구·상권 규모로
> 거의 설명되지 않는다는 뜻이고, 잔차가 크다는 뜻이므로 과부족 진단은 오히려 의미를 갖습니다.

### `recommend.py` (235줄) — 가설 4 · 추천/회피 동네

**하는 일** 잔차(주차 여유)에 **매력도**(음식점·카페·집객시설 백분위)를 결합한다.

**왜 잔차만으로 부족한가** 잔차가 큰 동네에는 놀거리 없는 주거지가 섞인다.
주차만 넉넉한 곳을 추천하면 서비스가 성립하지 않는다.

**핵심 수치** 추천 **23개**(6.4%) · 혼잡 주의 **21개** · 한산 31개 · 공영주차 없음 66개

```
혼잡 주의 상위  신촌동(음식점 1,132개에 46면) · 논현2동(927개에 13면) · 혜화동(525개에 8면)
```

**0면 66개 동을 되살린 이유** 기획서는 "별도 표시함"이라 했는데 코드에선 빠져 있었다.
이용자에게는 공영주차가 아예 없는 동네가 가장 먼저 알아야 할 정보다.
문정2동(음식점 821개, 0면)이 혼잡 주의 1위 신촌동(46면)보다 사정이 나쁘다.

### `cluster.py` (276줄) — 가설 2 · 동네 유형 × 시간대 패턴

**하는 일** 행정동을 유형으로 나누고, **유형별로 주차가 여유로운 때가 다른지** 본다.

**왜 절대량이 아니라 비율로 묶는가** — 중요한 판단

```
생활인구·음식점·집객시설이 서로 0.75~0.93 상관 → PC1이 사실상 '동네 크기 축'(로딩 .49/.49/.49)
그러면 '성격이 다른 동네'가 아니라 '큰 동네 vs 작은 동네'로 갈린다
→ 주거지수(상주÷(상주+직장)) + 천명당 밀도로 교체 (실루엣 0.249 → 0.268)
```

**핵심 수치** 실루엣 기준 **k=2** (어떤 피처 조합에서도 동일)

```
상권형 112개 (주거지수 0.60)   여유 일·밤(134)  빠듯 금·점심(86)   주말/평일 1.15
주거형 239개 (주거지수 0.88)   여유 화·점심(107) 빠듯 일·밤(91)    주말/평일 0.95
                                        ↑ 정확히 반대
```

> **Q. 기획서에는 3유형이라고 썼는데요?**
> 데이터가 지지하지 않습니다. 실루엣 계수 기준 k=2가 최적이고 어떤 피처 조합에서도 같습니다.
> **보고서에 3유형으로 쓰면 안 됩니다.**

**군집 번호를 안 쓰는 이유** K-means 번호는 실행마다 바뀐다. 보고서에 "유형 3"이라 쓰면
다음 실행에서 다른 동네를 가리킨다. 그래서 특성(주거지수·음식점 밀도)으로 **이름을 고정**한다.

### `timeslot_effect.py` (187줄) — 가설 5 · 시간대 조정 효과

**하는 일** 같은 동네를 **언제 가느냐만 바꿔도** 주차 여유가 달라지는지 검정한다.

**무엇이 변하는가** — 반드시 짚어야 할 부분

> 주차면수는 시간에 따라 변하지 않는다. `slots_per_1k`가 흔들리는 건 오직 분모인 생활인구 때문이다.
> 즉 **'주차장이 늘어난다'가 아니라 '같은 주차장을 두고 경쟁할 사람이 줄어든다'** 이다.

**왜 프리드먼 검정인가** 행정동마다 요일×시간대 값이 하나씩이라 **동 내부 분산이 없다.**
그래서 행정동을 블록으로 두는 비모수 반복측정 검정을 쓴다.

**핵심 수치** p = **1.3e-227** · 개선율 중앙값 **+34.5%** · +20% 이상 개선 **305/358개(85.2%)**

```
효과 TOP   소공동 +989% (수·점심 → 일·밤) · 회현동 +569% · 명동 +501% · 을지로동 +379%
           전부 업무지구 → 가설 2와 일관
```

> **Q. p값이 왜 이렇게 작죠?**
> n이 크면 사소한 차이도 유의해집니다. 그래서 **p값과 개선율 분포를 같이** 제시합니다.
> 실제로 쓸 만한지는 개선율 쪽이 답합니다.

### `real_demand.py` (232줄) — 가설 6 · 실수요 진단 ★ Day 2 신설

**하는 일** 수요 지표를 둘로 나눠 판정 차이를 본다.

```
유동수요 = 생활인구       나들이 이용자가 겪는 경쟁   (주 사용자)
정주수요 = 비아파트 가구   공영주차에 의존하는 가구    (부 사용자)
```

**확충 불필요 그룹을 먼저 떼는 이유** 비아파트 가구가 극소인 동은 '가구당 주차면'이
19,200까지 치솟아 순위를 지배한다. 그런데 이들은 지표 이상치가 아니라
**애초에 공영주차 실수요가 없는 동네**다. 제외가 아니라 별도 분류한다.

**임계값 100가구의 근거** — 반드시 나올 질문

```
정렬하면  ... 7, 7, 35, 40 │ 101, 129, 149 ...   ← 40과 101 사이 61가구 격차
41~100 어느 값을 잡아도 같은 13개 동이 분리됨 (임계값이 결과를 좌우하지 않음)
아파트 비율 99% 이상(12개 동)으로 교차 검증됨
```

**가설 6 판정 — 지지** 확충 불필요 9개 동(주차장 보유분) 중 **4개가 생활인구 기준으로는
'공급부족'** 으로 잡혔다.

```
상계9동 비아파트 4호 · 상계8동 2호 · 상계10동 35호 · 오륜동 7호
→ 비아파트 2~35호뿐인 아파트 단지가 확충 대상으로 지목되고 있었음
```

**판정이 갈리는 이유는 둘** — 정책 대응이 정반대라 구분해야 한다

```
아파트 흡수형(3)  잠실3동(97%)·역삼2동(61%)·당산2동(52%)  → 확충 우선순위를 내려야
방문객 수요형(2)  신촌동(11%)·화양동(9%)                  → 거주자우선이 아닌 시간제로
```

**정직하게 남긴 것** R² 유동 **0.068** / 정주 **0.004**. 실수요로 바꿔도 설명력은 안 오른다.

> 이것 자체가 결론입니다. **어떤 수요 지표로도 공영주차 배치가 설명되지 않는다**는 뜻이고,
> 현재 배치가 수요 기반이 아니므로 확충 우선순위를 수요 기준으로 재설정할 근거가 됩니다.
> 비아파트 가구는 설명력 개선용이 아니라 **오탐 제거용**으로 씁니다.

### `suitability.py` (196줄) — 나들이 적합도 지수 ★ Day 2 신설

**하는 일** 기획서가 약속한 **PCA 기반 지수**를 만든다.

**왜 만들었나** `recommend.py`의 종합점수가 `(매력도 + 주차)/2`로 **임의 가중치**였다.
기획서는 "임의 가중치가 아닌 PCA 기반"이라고 두 번 명시했는데 어긋나 있었다.

**로딩을 확인한 결과** — 지수의 정당성 근거

```
PC1 (52.1%)  음식점 +.582 · 카페 +.584 · 집객시설 +.548 │ 주차 −.13, −.04  → 매력도 축
PC2 (39.2%)  천명당주차면 +.694 · 주차여유 +.708        │ 매력도 ~.1      → 주차 여유 축
                                                          두 축 합계 91.3%
```

> 축을 사람이 정한 게 아니라 **데이터가 스스로 두 개로 나눴습니다.** 그게 "임의 가중치가 아니다"의 근거입니다.

**부호를 고정하는 이유** 고유벡터는 ±가 모두 해라 실행마다 뒤집힐 수 있다.
보고서 숫자가 실행할 때마다 바뀌면 안 되므로 기준 변수(PC1=음식점, PC2=천명당주차면)의
로딩이 양수가 되도록 고정한다.

**왜 기하평균인가** 나들이는 **약한 쪽이 병목**이다.

```
대학동   매력도 90.5 · 주차 1.7  →  산술평균 46.1  vs  기하평균 12.3
면목5동  매력도  2.8 · 주차 98.0 →  산술평균 50.4  vs  기하평균 16.6
```

산술평균이면 둘 다 "평균 동네"인데 실제로는 갈 수 없는 곳이다.

**핵심 수치** 적합도 TOP5 — 종로1.2.3.4가동 98.5 · 가산동 96.8 · 사직동 96.6 · 한강로동 95.1 · 양재1동 93.3

### `district.py` (167줄) — 자치구 요약 ★ Day 2 신설

**왜 필요한가** 확충을 집행하는 주체는 **자치구**인데 산출물이 전부 행정동 단위여서
담당자가 424행을 직접 훑어야 했다.

**비교 지표 주의** 합계는 큰 구가 자동으로 상위에 온다. 그래서 규모를 나눈
**'실수요 100가구당 주차면'** 으로 비교한다.

**핵심 수치**

```
공급 부족   관악 1.3 · 은평 1.4 · 광진 1.5 · 서대문 1.8
공급 여유   중구 14.2 · 강남 7.8 · 서초 7.7 · 종로 7.6
확충 후보   관악 7개(대학동·중앙동·서림동·서원동) · 종로 5개 · 성북 4개
확충 불필요 송파 5 · 노원 4 · 서초 2   ← 예산 배분에서 빼야 할 곳
```

### `maps.py` (146줄) — 공간 분포 지도 ★ Day 2 신설

**왜 필요한가** 순위표와 산점도만으로는 **어디에 몰려 있는지**가 안 보인다.

**좌표계 주의** SGIS 경계는 헤더에 CRS84(경위도)라고 선언하지만 실제로는
UTM-K(EPSG:5179) 미터 좌표를 준다. 경도 953,553도에 그려져 화면 밖으로 이탈한 적이 있다.
좌표값이 1,000을 넘으면 미터 좌표로 판별해 재투영한다.

**핵심 수치** 확충 후보 **52개 동이 22개 자치구에 분포**하며 관악·종로·성북에 집중

---

## 4. 공용 — `src/utils/`


| 파일            | 하는 일                               | 알아둘 것                                                      |
| ----------------- | --------------------------------------- | ---------------------------------------------------------------- |
| `settings.py`   | 경로·config·env 로더                | 모든 모듈이 여기서 경로를 받아 폴더를 옮겨도 한 곳만 고치면 됨 |
| `timeslot.py`   | 요일/시간대 파생                      | 구간 정의는`config.yaml` 한 곳에서만 관리                      |
| `holidays.py`   | 공휴일 판정                           | 2026년 제헌절 부활·지방선거 임시공휴일 반영                   |
| `plotstyle.py`  | 한글 폰트 자동 선택 · 라벨 겹침 방지 | Windows/macOS 양쪽에서 동작                                    |
| `api_client.py` | 공통 호출·페이지네이션·재시도       |                                                                |
| `logger.py`     | 로깅                                  |                                                                |

---

## 5. 대시보드 — `dashboard/`

**구조** `st.navigation` 라우터. 파일명이 아니라 코드로 라벨을 정하므로
사이드바 이름이 내용과 어긋나지 않고, **두 사용자를 나눠 보여줄 수 있다.**

```
app.py              라우터 (실행: streamlit run dashboard/app.py)
common.py           데이터 로딩 · 팔레트 · 사이드바 · 공용 헬퍼
views/
  overview.py       🅿️ 현황          KPI · 시간대별 생활인구 · 자치구 여유도 · 동네 순위
  map.py            🗺️ 지도          10개 지표 공간 분포 (절대값/편차 모드)
  recommend.py      🎯 동네 추천     사분면 · 적합도 랭킹 · 최적 시간대
  types.py          🧩 동네 유형     PCA·군집 · 유형별 시간대 패턴
  expansion.py      🏗️ 확충 우선순위  ← 부 사용자 전용
```

**설계 판단 다섯**


| 판단                                          | 이유                                                      |
| ----------------------------------------------- | ----------------------------------------------------------- |
| 분석 결과를**다시 계산하지 않고 읽기만** 한다 | 필터를 바꿔 값이 흔들리면 보고서 수치와 어긋난다          |
| 색은`common.py` **한 곳**에서 역할로만 배정   | 같은 색이 페이지마다 다른 뜻을 갖지 않게                  |
| 필터는**페이지가 쓰는 것만** 표시             | 안 쓰는 조건이 떠 있으면 바꿔도 화면이 안 변해 혼란스럽다 |
| 0면 처리는**맥락별 자동** (`with_parking`)    | 지표마다 답이 달라 전역 토글로는 안 된다                  |
| 확충 순위는**직접 지표**                      | 잔차는 회귀 보정이 거의 없어 주차면수와 순위상관 0.997    |

> **Q. 대시보드에서 직접 계산하면 안 되나요?**
> 사이드바 조건을 바꿀 때마다 순위가 달라지면 보고서에 적은 수치와 화면이 달라집니다.
> 실제로 전에는 코스추천 페이지가 `(매력도+주차)/2`를 즉석 계산해 그 문제가 있었습니다.

---

## 6. 산출물 목록

**`data/processed/` 8종**


| 파일                   | 규모         | 만든 스크립트   | 내용                       |
| ------------------------ | -------------- | ----------------- | ---------------------------- |
| `panel.csv`            | 14,840 × 31 | build_panel     | ★ 모든 분석의 입력        |
| `dong_residual.csv`    | 358 × 12    | residual        | 잔차·등급 (0면 66개 제외) |
| `dong_recommend.csv`   | 423 × 17    | recommend       | 추천/혼잡/한산/0면 등급    |
| `dong_cluster.csv`     | 351 × 17    | cluster         | 유형·주거지수             |
| `dong_timeslot.csv`    | 358 × 10    | timeslot_effect | 동별 최적·최악 시점       |
| `dong_real_demand.csv` | 422 × 17    | real_demand     | 실수요 판정·확충 필요성   |
| `dong_suitability.csv` | 357 × 14    | suitability     | 나들이 적합도 지수         |
| `sgg_summary.csv`      | 25 × 12     | district        | 자치구 요약                |

**`reports/figures/` 12종** — `.gitignore` 대상이라 스크립트로 재생성

```
corr_heatmap · weekday_timeslot · gu_distribution   (explore)
daily_trend                                          (daily_trend)
residual                                             (residual)
recommend                                            (recommend)
cluster                                              (cluster)
timeslot_effect                                      (timeslot_effect)
real_demand                                          (real_demand)
suitability                                          (suitability)
district                                             (district)
map_overview                                         (maps)
```

---

**파일 아키텍처 (전체 구조)**

```
seoul-parking/
├── config/config.yaml              시간대 5구간·회귀 임계값·상권 파일명 매핑
├── .env / .env.example             API 키 4종
│
├── data/                           ※ .gitignore — 실제 데이터는 깃에 없고 스크립트로 재생성
│   ├── raw/                        원본 (손대지 않음)
│   │   ├── living_population/*.csv   56일치, 1파일 = 1일(424동 × 24시간)
│   │   ├── parking_standard.csv      전국주차장 표준데이터(서울분) — 주(主)
│   │   └── parking_seoul.csv         서울시 공영주차장 API — 보충
│   ├── external/                   외부 참조 데이터
│   │   ├── dong_boundary.geojson     SGIS 행정동 경계 426개
│   │   ├── dong_crosswalk.csv        ← dong_code.py 산출
│   │   ├── adm_code/SEOUL_ADMI/*.csv 43개월치 코드정보 (크로스워크의 다리)
│   │   └── commercial/*.csv          점포·집객시설·상주인구·직장인구 (수동 다운로드)
│   ├── interim/                    중간 가공물
│   │   ├── parking_geocoded_standard.csv
│   │   └── parking_geocoded_seoul.csv ← geocode_parking.py 산출 (geo_adm_cd 부여)
│   └── processed/                  분석용 최종 테이블
│       ├── panel.csv                 ★ 14,840행 = 424동 × 7요일 × 5시간대
│       ├── dong_residual.csv         ← residual.py 산출 (잔차·등급) · 358행
│       ├── dong_cluster.csv          ← cluster.py 산출 (유형·주거지수) · 351행
│       ├── dong_recommend.csv        ← recommend.py 산출 (등급·매력도) · 423행
│       └── dong_timeslot.csv         ← timeslot_effect.py 산출 (최적/최악 시점) · 358행
│
├── src/
│   ├── collect/                    수집 (OpenAPI)
│   │   ├── fetch_living_population.py / fetch_parking_standard.py
│   │   ├── fetch_parking_seoul.py    / fetch_dong_boundary.py
│   │   └── fetch_poi_kakao.py        (미사용 — 403, 교차검증용으로만 남김)
│   ├── preprocess/                 ★ 가장 오래 걸린 단계
│   │   ├── dong_code.py              코드 크로스워크 (7.5%→100%)
│   │   ├── geocode_parking.py        주차장 행정동 배정 (100%)
│   │   ├── load_commercial.py        상권 CSV 4종 로드 (인코딩 자동판별)
│   │   ├── build_panel.py            집계·병합·파생·공휴일 제외 → panel.csv
│   │   └── validate.py               자동 품질검증
│   ├── analysis/                    ※ recommend 는 residual 산출물을 읽음 (실행 순서 주의)
│   │   ├── daily_trend.py            일별 추세·공휴일 검증 (raw 필요)
│   │   ├── explore.py                상관 히트맵·자치구 분포 + 0면 민감도 (가설 1·3)
│   │   ├── residual.py               회귀 잔차 (핵심 결과물)
│   │   ├── cluster.py                동네 유형 × 시간대 패턴 (가설 2)
│   │   ├── recommend.py              매력도 × 주차 여유 + 0면 경고 (가설 4)
│   │   └── timeslot_effect.py        시간대 조정 효과·프리드먼 검정 (가설 5)
│   └── utils/
│       ├── settings.py               경로·config·env 로더
│       ├── timeslot.py               요일/시간대 파생
│       ├── holidays.py               공휴일 정의·판정
│       ├── plotstyle.py              한글 폰트 자동 선택 · 라벨 겹침 방지
│       ├── api_client.py / logger.py
│
├── dashboard/                      Streamlit 4페이지
│   ├── app.py                        탐색 (KPI·시간대 곡선·자치구 여유도)
│   ├── common.py                     사이드바 등 공통
│   └── pages/  1_지도.py · 2_코스추천.py · 3_동네유형.py
│
├── docs/
│   ├── data_sources.md / data_links.md / data_dictionary.md
│   └── methodology.md                회귀 잔차 방법론 (로그변환 근거 등)
│
├── reports/
│   ├── report.md                     ⬜ 목차 상태
│   └── figures/*.png                 정적 그래프 8종 (.gitignore — 스크립트로 재생성)
│
├── submission/                     제출용 (01_보고서 ~ 04_스크린샷)
├── data/README.md                  ★ 데이터 전체 안내 (출처·흐름·처리·한계)
├── README.md / PROGRESS.md
└── 멘토링_브리핑.xlsx               사전질문·질문리스트·목표·그래프·핵심수치 5시트
```

**데이터 흐름 한 줄**
`raw/` + `external/` → `dong_code.py`·`geocode_parking.py` → `interim/` → `build_panel.py` → `processed/panel.csv` → `validate.py` 검증 → `analysis/`가 읽어 그래프·잔차 산출

---

**데이터 실물 — 어디에 얼마나 있는가** (2026-08-11 실측)


| 폴더              | 파일 수 | 용량        | 성격                        |
| ------------------- | --------- | ------------- | ----------------------------- |
| `data/raw/`       | 59개    | **158.2MB** | API 원본. 절대 손대지 않음  |
| `data/external/`  | 52개    | 33.3MB      | 외부에서 받은 참조 데이터   |
| `data/interim/`   | 3개     | 1.3MB       | 중간 가공물 (지오코딩 결과) |
| `data/processed/` | 3개     | 2.6MB       | 분석용 최종 테이블          |


| 파일                                      | 용량        | 행수                  | 비고                                                   |
| ------------------------------------------- | ------------- | ----------------------- | -------------------------------------------------------- |
| `raw/living_population/*.csv`             | **157.1MB** | 56개 파일 × 10,176행 | 파일당 2.8MB.`20260602.csv` ~ `20260727.csv`           |
| `raw/parking_standard.csv`                | 324KB       | 856                   | 주(主) 주차장 데이터                                   |
| `raw/parking_seoul.csv`                   | 743KB       | 2,189                 | 보충용                                                 |
| `external/commercial/점포_행정동.csv`     | **13.6MB**  | 176,531               | 업종 100종 × 행정동이라 행이 많음                     |
| `external/commercial/상주인구_행정동.csv` | 1.7MB       | 8,925                 |                                                        |
| `external/commercial/직장인구_행정동.csv` | 1.3MB       | 8,694                 | 414개 동만 제공(결측 사유)                             |
| `external/commercial/집객시설_행정동.csv` | 886KB       | 8,925                 |                                                        |
| `external/adm_code/SEOUL_ADMI/ADMI_*.csv` | 13.0MB      | 43개 파일             | `ADMI_202301` ~ `ADMI_202607`                          |
| `external/dong_boundary.geojson`          | 1.0MB       | 426 폴리곤            |                                                        |
| `external/dong_crosswalk.csv`             | 32KB        | 426                   |                                                        |
| `interim/parking_geocoded_standard.csv`   | 390KB       | 856                   | 원본 +`geo_*` 6컬럼                                    |
| `interim/parking_geocoded_seoul.csv`      | 897KB       | 2,189                 | 〃 (서울시 API분)                                      |
| `processed/panel.csv`                     | 2.5MB       | **11,872**            | ★ 최종 산출                                           |
| `processed/dong_residual.csv`             | 53KB        | **358**               | 0면 66개 동 제외된 수                                  |
| `processed/dong_cluster.csv`              | —          | **351**               | Day 2 신설. 유형 분류 (상권형 114 / 주거형 237)        |
| `processed/dong_recommend.csv`            | —          | **423**               | Day 2 신설.**0면 66개 동 포함** (매력도 결측 1개 제외) |
| `processed/dong_timeslot.csv`             | —          | **358**               | Day 2 신설. 동별 최적·최악 시점과 개선율              |

> ⚠️ `data/README.md`에는 생활인구가 "총 118MB"로 적혀 있으나 **실측 157.1MB**입니다. 수집 중 갱신 누락 — 문서 수정 필요.
>
> ⚠️ Day 2에 추가한 `dong_cluster` · `dong_recommend` · `dong_timeslot` 3종은 **`.gitignore` 대상**입니다.
> 대시보드가 아직 쓰지 않기 때문인데, 정적 결과를 대시보드에 붙이려면 `panel.csv`처럼 예외 처리가 필요합니다.

**왜 파일명을 이렇게 지었나**


| 규칙                                  | 예시                                         | 이유                                                                                                                                                                                                                          |
| --------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`data/`를 4단계로 분리**            | `raw` / `external` / `interim` / `processed` | Cookiecutter Data Science 관례.**"원본은 절대 덮어쓰지 않는다"** 를 폴더 구조로 강제하기 위함. 재현 시 어디부터 다시 돌릴지가 폴더만 봐도 결정됨                                                                              |
| **날짜 자체가 파일명**                | `living_population/20260602.csv`             | 파일명 = 기본키(수집일). ① 정렬하면 시간순 ②`out_path.exists()` 로 **이미 받은 날은 건너뛰어** 중단 후 이어받기 가능(멱등) ③ `glob('*.csv')` 만으로 전체 로드                                                              |
| **접미사 = 출처**                     | `parking_standard` / `parking_seoul`         | 주차장 데이터가 2종이고**성격이 달라**(표준=거주자우선 제외, 서울시=포함) 파일명에서 바로 구분되어야 함. `geocode_parking.py`의 `SOURCES` 딕셔너리 키·`--source` 인자값과 **같은 단어**로 통일                               |
| **가공 단계를 이름에 명시**           | `parking_geocoded_standard.csv`              | `parking_standard`(원본) → `parking_geocoded_standard`(지오코딩 후). 원본과 나란히 놓아도 어느 단계인지 구분됨                                                                                                               |
| **상권 CSV는 한글 원본명 유지**       | `점포_행정동.csv`                            | 수동 다운로드라**사이트에서 받은 이름 그대로** 두어야 재다운로드 시 헷갈리지 않음. `-행정동`(OA-22xxx)과 `-상권`(OA-15xxx)은 공간단위가 달라 **접미사가 곧 경고 역할**. 실제 경로는 `config.yaml`의 `commercial.files`가 관리 |
| **코드표는 `ADMI_YYYYMM`**            | `ADMI_202301.csv`                            | 배포처 원본명. 43개월분을**전부 합쳐 써야** 함 — 통폐합으로 특정 월에만 존재하는 코드가 있어서(용신동·일원2동)                                                                                                              |
| **경로 상수를 코드에 하드코딩 안 함** | `settings.py`의 `DATA_RAW` 등                | 모든 모듈이`from src.utils.settings import DATA_RAW` 로 동일 경로 사용. 폴더를 옮겨도 한 곳만 고치면 됨                                                                                                                       |
| **데이터는 깃에 안 올림**             | `.gitignore`: `data/*/*` + `!.gitkeep`       | 158MB라 저장소에 부적합.**`.gitkeep`만 남겨 폴더 구조는 보존** → 클론 후 스크립트만 돌리면 재생성                                                                                                                            |

---

****전처리 어떻게 했냐"고 물었을 때 열 파일을 우선순위로 정리했습니다.**


| 순서 | 파일                                | 여기서 답하는 것                                                                                                                                                                         |
| ------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | `data/README.md` §5                | **먼저 말할 한 장 요약.** 핵심 처리는 ① 코드 크로스워크 ② 주차장 지오코딩 두 가지                                                                                                      |
| 2    | `src/preprocess/dong_code.py`       | 코드 체계 불일치 해결 (조인율 7.5%→100%). 예외처리 3종이 전부 여기 —`normalize_dong_name`(가운뎃점 표기), `resolve_legacy_codes`(구버전 코드 순서매칭), `SPLIT_DONGS`(상일동 1:2 분할) |
| 3    | `src/preprocess/geocode_parking.py` | 주차장에 행정동이 없어 주소로 배정. 좌표 결측(표준 5.7%·서울시 33.5%) 때문에 공간조인 대신 SGIS 지오코딩 → 배정률 100%                                                                 |
| 4    | `src/preprocess/build_panel.py`     | 집계·병합·파생변수(`slots_per_1k`), **공휴일 2일 제외**(변동계수 수 2.01%→0.19%)                                                                                                      |
| 5    | `src/preprocess/validate.py`        | "품질 검증했냐" 대응. 구조/중복/논리/결측/이상치 5종. 이상치를 안 지운 이유도 여기                                                                                                       |
| 6    | `docs/methodology.md` §3           | "왜 로그변환?" 대응. 원단위 vs 로그-로그 왜도·등분산 비교표                                                                                                                             |
| 7    | `멘토링_브리핑.xlsx` `5_핵심수치`   | 숫자가 바로 안 나올 때 화면 공유용                                                                                                                                                       |

> `load_commercial.py`는 상권 CSV 읽기(인코딩 자동판별)까지만 하고 병합은 `build_panel.py`가 합니다.
> 파일 안에 TODO가 남아 있으니 질문 나오면 이렇게 답할 것.

---

## Day 1 — 2026-08-10 (월)

### ✅ 한 작업

**기획 확정**

- 분석 단위: **행정동 × 요일 × 시간대** (426 × 7 × 4 ≈ 11,928행)
- 시간대 4구간: 아침(06-11) / 점심(11-14) / 오후(14-18) / 저녁밤(18-24)
- 기획서 작성 후 **사실 검증으로 20여 곳 수정** (원본은 `_원본백업.docx`)

**환경**

- Python 3.14.7 + `.venv` (pandas 3.0.5, geopandas 1.1.4)
- API 키 4종 확보 (서울 열린데이터광장 / 공공데이터포털 / SGIS / 카카오)
- git 저장소 초기화, 커밋 10회

**데이터 수집 완료**


| 데이터          | 결과                                                                    |
| ----------------- | ------------------------------------------------------------------------- |
| 생활인구        | **56일** (2026-06-02~07-27) = 7개 요일 × 각 8일치, 118MB               |
| 공영주차장      | **78,267면 / 358개 행정동** (표준데이터 + 서울시 API 보충), 배정률 100% |
| 행정동 경계     | **426개**                                                               |
| 상권분석 4종    | 점포·집객시설·상주인구·직장인구                                      |
| 행정동 코드정보 | 43개월분 (크로스워크용)                                                 |

---

### 🔧 트러블슈팅 (핵심만)

**① 실시간 주차 데이터는 존재하지 않음**

- `GetParkInfo`의 `CUR_PARKING`(현재주차대수) 컬럼 **폐지 확인** ("추후 제공예정 없음")
- 대안(시영주차장 실시간)은 커버리지 협소 → **실시간 포기, 구조적 과부족(회귀 잔차)으로 방향 전환**
- 결과적으로 기획 의도("목적지 정하기 전 판단")에 더 부합

**② 행정동코드 체계 불일치 → 크로스워크로 해결**

- 생활인구(행안부 `11110530`) vs SGIS 경계(자체 `11010530`) → 직접 조인 시 **424개 중 32개만 일치**
- 서울 열린데이터광장 **'전국 행정동 코드정보'** 를 다리로 사용
  ```
  생활인구코드 → ADMI_CD → FULL_NM → SGIS adm_nm → 경계
  ```
- 처리한 예외: 가운뎃점 표기(`상계3.4동`↔`상계3·4동`) / 구버전 코드 7개 / 동 분할 1:2 / 월별 누락
- **7.5% → 100%** (인구 손실 0명)

**③ 주차장 위경도 결측 → SGIS 지오코딩**

- 좌표 공간조인만으로는 배정이 불안정 (서울시 API 33.5% 결측 / 표준데이터 5.7% 결측)
- SGIS `addr/geocodewgs84`는 좌표와 함께 **행정동코드를 직접 반환** → 공간조인 불필요
- 카카오 로컬 API는 앱 서비스 미활성으로 403 (문서엔 "활성화 불필요"로 적혀 있으나 실제와 다름)
- 주소 후보 폴백(지번 → 군더더기 제거 → 동 단위) + 특이 표기 정규화
  - 번지/호(`신사동산55번지5호`), 행정동명+지번 붙음(`상계6·7동770-2`), 산 지번(`산10-84`), 복수 주소(`A+B+C`)
- **배정률 100%** (856/856건, 73,796면), 경계코드 100% 일치

**④ 주차 데이터 소스를 표준데이터로 교체 (중요)**

- 초기에 서울시 공영주차장 API 사용 → 검증 중 문제 발견
  ```
  서교동(홍대)  서울시 API:   6면  ← 1면짜리 거주자우선 구획
                표준데이터 : 152면  ← 실제 주차장
  ```
- 표준데이터는 정의상 **"거주지우선주차지역 제외"** → 방문객 주차 분석에 적합
- 주차면도 73,796 vs 61,035로 더 많음 → **표준데이터로 교체 확정**

**⑤ 민영주차장 확보 불가 — 원인 규명**

- 공공데이터포털 API가 한때 장애(504)였다가 **복구 후 수집 성공**
- 그러나 서울 민영은 **45건(1,149면), 구로구 39·금천구 6뿐**. 나머지 23개 자치구 0건
- 원인 두 가지:
  1. 표준데이터 수집 범위가 **"지자체 관리 대상"** 으로 한정 → 민영 대부분 범위 밖
  2. 같은 기준인데 자치구별 등록 편차 (주차장법상 통보 의무가 있어 **데이터는 존재하나 미개방**)
- → **한계로 명시하고 진행.** 정책 제언 소재로 활용 가능

---

**⑧ 0면 행정동 26% 문제 → 데이터 병합 + 분석 프레임 전환**

- 패널 생성 후 확인: 표준데이터 단독으로는 **424개 동 중 109개(26%)가 주차면 0**
- 검증 결과 **필터링(제외)은 불가** — 0면 동이 생활인구 22.5%·음식점 18.5%를 차지하고
  그 안에 성수1가2동·성수2가3동·서초2동·석촌동 등 핵심 나들이 지역이 포함됨
- 대응 ①: **두 데이터 병합** — 표준데이터를 기본으로, **0면인 동에만** 서울시 API 보충
  (겹치는 동이 없어 이중 계산 없음. 거주자우선·버스전용은 제외)
  → **0면 109개 → 66개 (26% → 15.6%)**, 공급 73,796 → **78,267면**
  → 성수1가2동 329면 · 왕십리도선동 260면 등 복구
- 대응 ②: **분석 프레임 전환**
  민영·부설 누락은 0면 동만의 문제가 아니라 **전 지역 공통**이므로,
  측정 대상을 "전체 주차 공급"이 아닌 **"공영주차 접근성"** 으로 한정 서술
  → 결론을 "주차난"이 아니라 **"공영주차 인프라 부족"** 으로 표현
  → 정책 제언(공영주차장 확충 우선순위)으로는 오히려 더 직접적

---

### ⚠️ 남은 분석 리스크

**공영주차장 0면 행정동 66개(15.6%)** — 결측이 아닌 실제 0이나, 민영 미개방 영향이 섞여 있음.
→ 회귀·순위에서 `has_parking`으로 구분 처리하고 보고서에 명시할 것.

---

### 🔧 트러블슈팅 (추가)

**⑨ 일별 추세 분석 누락 → 공휴일 처리 추가**

- 문제: 56일을 요일×시간대로 곧바로 평균내 **날짜 축을 한 번도 보지 않았음**
- 출근시간(08~09시) 생활인구로 이상일을 탐지하니 **평일 2일**이 걸림
  - **2026-06-03(수) 제9회 전국동시지방선거** — 임시공휴일
  - **2026-07-17(금) 제헌절** — 2026년부터 공휴일 부활
- 두 날 모두 평일 대비 **-6.6%**, 주말(-6.2%)과 사실상 같은 수준
- 대응: 요일 평균에서 **평일 공휴일 제외** (현충일 6/6은 토요일이라 유지)
- 효과: 변동계수 **수 2.01%→0.19%, 금 2.97%→0.38%**
- 6월→7월 추세 변화는 -0.5%로 무시 가능
- 산출: `src/utils/holidays.py`, `src/analysis/daily_trend.py`, `daily_trend.png`

**⑩ 지도에 폴리곤이 안 그려짐 → 좌표계 재투영**

- SGIS 경계가 헤더에 CRS84(경위도)라고 선언하고 실제로는 UTM-K 미터 좌표를 줌
- Plotly가 경도 953,553도에 그려 화면 밖으로 이탈 → 배경 지도만 보임
- 대응: 좌표값으로 판별해 EPSG:5179→4326 재투영, 검증 항목에도 추가

**⑪ 브라우저 자동 번역으로 한글 훼손**

- Streamlit이 `<html lang="en">`으로 내보내 크롬이 한국어를 다시 번역
  ('시간대'→'대처', '넉넉한/빠듯한'→'제거한/마치 같은')
- 대응: `components.html`로 부모 문서의 `lang="ko"`·`translate="no"` 주입

---

### 📌 남은 작업

- [X]  ~~패널 테이블 생성~~ — `data/processed/panel.csv` **11,872행 × 22열**
- [X]  ~~상권 CSV 병합~~ (20261 분기)
- [X]  ~~분석: 회귀 잔차 / 상관 히트맵 / 일별 추세~~
- [X]  ~~Streamlit 대시보드 4페이지~~
- [ ]  **보고서** (`reports/report.md` 목차 상태)
- [ ]  **발표자료**

---

### 🔜 Day 2에 볼 것 (우선순위순)

**A. 반드시 해야 하는 것**

1. **보고서 작성** — 재료(그림 5종, R²=0.074, 공급부족 56개 동)는 다 나와 있음
2. **발표자료** — 대시보드 시연 흐름 포함

**B. 분석 구멍 — 기획서 가설 5개 중 2개만 검증됨**


| 가설                                        | 상태                                            |
| --------------------------------------------- | ------------------------------------------------- |
| 1. 유동인구↑ → 1인당 공영주차면↓         | ✅ 검증 (r=-0.14, 매우 약함)                    |
| 2. 상주/직장인구로 동네 유형 구분           | ⚠️ 대시보드에만 있고**정적 결론·그림 없음**  |
| 3. 20~30대·음식점 밀도 ↔ 주차면 음의 상관 | ✅ 기각 (r=-0.02)                               |
| 4. 회귀선 상방 예외지역 = 추천 대상         | ⚠️ 잔차 상위는 뽑았으나**매력도 조건 미결합** |
| 5. 시간대 조정으로 혼잡 유의하게 감소       | ⚠️ 대시보드엔 있으나**정량 결론 없음**        |

→ 가설 2·4·5를 정적 분석으로 마무리해야 보고서에 넣을 수 있음

**C. 품질 개선**

- **테스트 0개** — 크로스워크·패널 생성 같은 핵심 로직에 회귀 테스트 필요
- `docs/data_sources.md`·`data_dictionary.md`가 낡음 (6종 시절·민영 서버장애 시절 서술)
- 수집해둔 **주차요금(`avg_fee_per_hour`)을 분석에 거의 활용 안 함**
- 대시보드 `3_동네유형`에서 실루엣 계산(KMeans 6회)이 **캐시 밖**이라 필터 바꿀 때마다 재계산

**D. 발표 리스크**

- 브라우저 자동 번역으로 한글이 깨질 수 있음 → 시연 전 번역 끄기 확인
- 지도 페이지 첫 로딩 2~3초 → 미리 띄워두기

---

## Day 2 — 2026-08-11 (화)

### ✅ 한 작업

**1. 기획서 개정 — 부사용자를 빼고 주 사용자만 남김**

부사용자(행정)를 두니 "공영주차장을 어디에 지을까 → 지을 때 뭘 고려할까"로 계속 파고들어
초점이 흐려졌다. 사용자를 한 명으로 좁혔다.

> **주 사용자** — 지도 앱에 가고 싶은 곳을 다수 저장해 둔 자가용 이용자.
> 후보지가 많아 목적지를 고집하지 않으며, 만차로 허비하는 시간을 피하는 것이 우선.
> 요일·시간대는 자유롭게 조정할 수 있다.
> → "어디에 갈지"가 아니라 **"언제 갈 수 있는지"를 먼저 정하고** 그때 여유로운 동네를 고른다.


| 항목        | 변경                                                                                                             |
| ------------- | ------------------------------------------------------------------------------------------------------------------ |
| 아이디어명  | 주차수급 불균형**진단** 및 취약지역 **도출** → **생활인구 기반 서울 나들이 주차 여유 동네·시간대 추천 서비스** |
| 신설        | `2. 주 사용자` 절 (이후 절은 3·4로 번호 이동)                                                                   |
| 문제점 ③   | 공급 불균형의 비가시성(~행정의 우선순위) →**선택 기준의 부재**                                                  |
| 추진목적 ② | 주차 취약 지역 도출 →**추천·회피 동네 구분**                                                                   |
| 추진목적 ③ | 실행 가능한 대안 →**개인화된 나들이 코스 제시**                                                                 |
| 분석목표 ② | 공영주차 접근성 진단 →**공영주차 여유도 산출**                                                                  |
| 기대효과 ② | 정책적 활용(확충 우선순위)**삭제** → **탐색 비용 절감**                                                         |
| 가설 2      | "유형으로 구분될 것" →**"유형에 따라 주차 여유의 요일·시간대 패턴이 다를 것"**                                 |

**분석은 버리지 않았다.** 잔차 결과는 읽는 방향만 뒤집으면 그대로 쓰인다.
공급여유 = "여기로 가세요", 공급부족 = "여긴 피하세요". 가설 1·3도 진단 근거가 아니라
**"인기 있는 동네를 무작정 고르면 손해인 이유"** 로 바뀌어 서비스의 존재 이유가 된다.

**2. 개인화 컨셉 추가 — 다만 자동 연동은 불가능함을 확인**

"네이버 지도에 저장해둔 곳을 웹사이트로 불러올 수 있나"를 조사한 결과 **둘 다 막혀 있었다.**


| 원하던 것                  | 결과                   | 근거                                                                                                                                |
| ---------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| 네이버 저장 목록 자동 로드 | ❌**공식 API 없음**    | 네이버가 여는 건 지도 표시·지오코딩·길찾기뿐. 네아로 OAuth도 프로필만 제공하고 MY플레이스 스코프 없음                             |
| 카카오맵 평점 기반 추천    | ❌**정책상 제공 불가** | 카카오 공식 답변: "내부 정책 상 POI 내 평점, 리뷰 데이터 등 장소 상세 정보는 오픈API로 제공하지 않습니다" (제휴로도 검토 불가 범위) |

- 카카오 Local API 응답 필드: `id · place_name · category_name · phone · address_name · road_address_name · x · y · place_url · distance` — **평점·리뷰 없음**
- 네이버 앱에 "저장 리스트 공유" 기능은 있으나 네이버 앱/웹용이지 제3자 파싱용이 아님 → 스크래핑은 약관·안정성 양쪽에서 배제

**대체 설계** (기획서 반영 완료)

```
[불가] 네이버 계정 연동 → 저장 목록 자동 로드
[채택] 사용자가 장소명 목록 붙여넣기/업로드
       → 카카오 Local 키워드 검색으로 좌표 획득 → SGIS로 행정동 매칭
       → 요일·시간대별 주차 여유도와 결합
평점 대체 → 이미 가진 상권 데이터(음식점·카페·집객시설 밀도) + place_url 링크 연결
```

→ 한계 2건을 기획서 비고에 명시. 민영주차장 미개방과 같은 결의 정직한 한계로 읽힌다.

**3. 가설 2·4·5 정적 스크립트 신설 — 대시보드에만 있던 분석을 보고서용으로**

사이드바 필터에 따라 값이 바뀌면 보고서에 인용할 수치가 없다. 고정 산출물로 옮겼다.


| 스크립트                          | 가설 | 결과                                                                                                            |
| ----------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------- |
| `src/analysis/cluster.py`         | 2    | **지지** — 상권형 114개(주거지수 0.60) / 주거형 237개(0.88). 주말/평일 여유도가 **1.20 vs 0.95로 방향이 반대** |
| `src/analysis/recommend.py`       | 4    | **지지** — 추천 23개(6.4%) / 혼잡 주의 21개                                                                    |
| `src/analysis/timeslot_effect.py` | 5    | **지지** — 프리드먼 p=9.6e-181, 개선율 중앙값 **+31.3%**, +20% 이상 개선 289/358개(80.7%)                      |

- **가설 2에서 피처를 절대량 → 비율로 교체.** 생활인구·음식점·집객시설은 서로 0.75~0.93 상관이라
  PC1이 사실상 **규모축**(로딩 0.49/0.49/0.49)이 되어 "성격이 다른 동네"가 아니라
  "큰 동네 vs 작은 동네"로 갈렸다. 기획서의 "상주·직장인구 **비율** 기준"과도 어긋남
  → 주거지수 + 천명당 밀도로 교체 (실루엣 0.249 → 0.268)
- **유형은 2개뿐.** 어떤 피처 조합에서도 k=2가 최적. 기획서가 예상한 3유형(직장·주거·상권)은
  데이터가 지지하지 않으므로 **보고서에 3유형으로 쓰면 안 됨**
- 상권형 최적 **일·아침(133)** / 최악 화·점심(86), 주거형은 정확히 뒤집힘 → 발표용 한 장으로 좋음
- 가설 5 상위권(소공동 +636%, 회현동 +414%, 을지로동 +367%)이 전부 업무지구라 가설 2와 일관됨
- K-means 군집 번호는 실행마다 바뀌므로 특성 기반으로 **이름을 고정**해 저장

**4. 그림 8종 확보** — 기존 5종 + `cluster` · `recommend` · `timeslot_effect`

**5. 기획 재개정 — 부사용자 복원 + 거주형태 도입** (오전 개정을 되돌림)

오전에 사용자를 하나로 줄였더니 **분석이 아니라 서비스 기획**이 됐고,
핵심 기능(관심 장소 매칭)은 네이버·카카오 API 미제공으로 구현이 불가능했다.
공무원을 부사용자로 되살리면서 **거주 형태**라는 변수를 하나 명확히 추가했다.

```
유동수요 = 생활인구        나들이 이용자가 겪는 경쟁    (주 사용자)
정주수요 = 비아파트 가구    공영주차에 의존하는 가구     (부 사용자)
```

- 데이터: KOSIS 인구주택총조사 2025 「거처의 종류별 가구」 서울 427개 동
- 전처리: `load_census.py` 신설 → 424/424 (100%) 매칭
- 분석: `real_demand.py` 신설 → **가설 6 지지**

**6. 분석 4종 추가** — 기획서가 약속했는데 빠져 있던 것들


| 스크립트            | 채운 구멍                                                                                    |
| --------------------- | ---------------------------------------------------------------------------------------------- |
| `suitability.py`    | 「나들이 적합도 지수」가 기획서에 두 번 명시됐는데 실제로는`(매력도+주차)/2` 임의 가중치였다 |
| `district.py`       | 확충을 집행하는 주체가 자치구인데 산출물이 전부 행정동 단위였다                              |
| `maps.py`           | 공간 분포 정적 그림이 없었다                                                                 |
| `cluster.py` 레이더 | 기획서 분석방법에 명시돼 있었으나 대시보드에만 있었다                                        |

**7. 대시보드 개편**

```
색      역할 기반 팔레트로 통일 (크기=파랑 / 극성=빨강↔회색↔파랑 / 상태=경고만 빨강)
구조    pages/ 자동탐색 → st.navigation. 사이드바를 두 사용자로 분리
필터    페이지가 쓰는 것만 표시
0면     전역 토글 제거 → 맥락별 자동 처리
지표    확충 우선순위를 잔차 → 직접 지표(비아파트 100가구당 주차면)로
```

---

### 🔧 트러블슈팅

**⑫ 지오코딩 산출 파일명 불일치 — 클린 환경에서 재현 실패** ✅ 수정 완료

- 증상: `geocode_parking.py:240` 은 `parking_geocoded_{source}.csv` 로 저장 → `--source seoul` 이면
  **`parking_geocoded_seoul.csv`** 인데, `build_panel.py:128` 은 **`parking_geocoded.csv`**(접미사 없음)를 읽고 있었음
- 그동안 `interim/` 에 접미사 없는 옛 파일이 남아 있어 **우연히 동작**하고 있었음
- 영향: 클린 환경에서 README 재현 절차대로 돌리면 보충 파일을 못 찾아
  `"서울시 공영주차장 지오코딩 결과가 없어 보충을 건너뜁니다"` 경고와 함께
  **43개 동 보충분이 통째로 누락** → 78,267면 → 73,796면, 0면 동 66 → 109개
- **조치 3가지**
  1. `data/interim/parking_geocoded.csv` → **`parking_geocoded_seoul.csv`** 로 이름 변경 (내용 확인 후: `OPER_SE_NM`·`TPKCT` 보유 = 서울시 API분 맞음)
  2. `build_panel.py:128` 경로를 `parking_geocoded_seoul.csv` 로 수정
  3. `geocode_parking.py` docstring의 옛 경로도 `parking_geocoded_{source}.csv` 로 정정
- **검증**: `build_panel` 재실행 후 이전 `panel.csv` 와 대조 → `assert_frame_equal` **완전 동일**
  (11,872행 × 22열 / 78,267면 / source 분포 standard 315·seoul_api 43·none 66 모두 일치)
  `validate.py` 전 항목 통과 (exit 0)

> 교훈: 산출 경로를 f-string으로 만드는 쪽과 읽는 쪽이 **문자열로 따로 적혀 있으면** 어긋나도 즉시 드러나지 않는다.
> 옛 산출물이 남아 있어 로컬에서만 통과하는 형태라 **커밋 전까지 발견이 어려웠음.**

**⑭ 공영주차 0면 66개 동이 모든 분석에서 누락** ✅ 수정 완료

- 기획서 비고는 **"66개(15.6%)는 0면으로 남아 별도 표시함"** 이라고 적었으나,
  `residual` · `recommend` · `cluster` · `timeslot_effect` **네 곳 모두 `has_parking`으로 걸러
  어디에도 표시되지 않고 있었다**
- 행정 관점에서는 타당했다(잔차 하위=데이터 부재를 주차난으로 오독 방지).
  그러나 **사용자 관점에서는 정반대** — 공영주차가 아예 없는 동네야말로 가장 먼저 알아야 할 정보다

```
혼잡 주의 1위 신촌동   46면   ← 목록에 있음
송파구 문정2동          0면   음식점 821개, 생활인구 44,299   ← 목록에 없었음
강동구 길동             0면   음식점 663개
서초구 서초2동          0면   음식점 541개
```

- 0면 동이 **생활인구 14.2% · 음식점 10.7%** 를 차지하고, 매력도 상위 25%에 10개가 들어 있음
- 조치: `recommend.py`에 **`공영주차 없음` 등급 신설.** 잔차를 구할 수 없으므로
  매력도만으로 순위를 매겨 별도 표·그림(1×3 확장, 산점도 하단 별도 띠)으로 제시
- 매력도 백분위 모집단도 358 → **423개**로 변경. 이용자가 갈 수 있는 곳 전부를 놓고
  따져야 "상위 몇 %"가 의미를 가짐 → 이 영향으로 혼잡 주의 20 → 21개

**⑮ 가설 1·3이 0면 제외 덕분에 강해 보이던 문제** ✅ 민감도 병기

`slots_per_1k = 0`은 결측이 아니라 실제 값이라 포함한 결과도 제시해야 "왜 뺐느냐"에 답할 수 있다.


| 변수 (vs 천명당주차면) | 제외 (현재)         | 포함                       |
| ------------------------ | --------------------- | ---------------------------- |
| 생활인구               | r = −0.138 (n=358) | r = −0.102 (n=424)        |
| 음식점수               | r = −0.022 (n=357) | **r = +0.027** (부호 반전) |
| 20~30대비중            | r = −0.116 (n=358) | r = −0.061 (n=424)        |

- 결론은 유지(가설1 매우 약함 · 가설3 기각)되나 **제외 시 음의 상관이 다소 강해 보임**
- 0면 동은 생활인구 4분위에 고르게 퍼져 있어(하위25% 18.9% → 상위25% 13.2%) 계통 편향은 아님
- `explore.py` 출력에 민감도 표를 추가해 실행할 때마다 함께 나오게 함

**⑰ 잔차를 '수요 대비 부족'으로 읽으면 안 된다** ✅ 표현·지표 정정

가장 중요한 발견이다. **설명력이 낮다는 것은 회귀선이 평평하다는 뜻**이고,
그러면 잔차는 '수요로 보정한 값'이 아니라 **주차면 수 그 자체**가 된다.

```
정주수요 기울기 0.082  →  비아파트 가구가 10배 늘어도 기대 주차면은 1.21배
z_정주 순위 ↔ 주차면수 순위   스피어만 0.997   하위 20개 중 19개 동일
z_유동 순위 ↔ 주차면수 순위   스피어만 0.959
```

- 대응 ①: 확충 우선순위를 **직접 지표**로 교체
  `slots_per_100_nonapt = 공영주차면 ÷ 비아파트 가구 × 100`
  "6,838가구에 1면"처럼 그대로 읽히고 보정한 척하지 않는다
- 대응 ②: 판정 기준(잔차 ≤ −1, 확충 후보 52개)은 **유지**. 보고서·PROGRESS 기록과 맞추기 위함
- 대응 ③: 지도에서 실수요 잔차 제거(주차면수와 중복), 남긴 잔차에는 경고 배너
- 대응 ④: `real_demand.py` docstring에 근거를 남기고 실행 시마다 검증값 출력

> **보고서에 반드시 쓸 것.** "R²가 낮은데 잔차를 왜 쓰냐"는 질문에 대한 답이자,
> 낮은 설명력 자체가 결론("배치가 수요 기반이 아니다")이라는 근거다.

**⑱ 대시보드 기본값이 분석 결론과 반대였다** ✅ 수정 완료

- 분석에서는 0면 66개 동을 "가장 먼저 알아야 할 정보"로 되살려 놓고,
  대시보드는 사이드바 체크박스 기본값으로 **숨기고** 있었다
- 원인: 지표마다 답이 다른 판단을 전역 토글 하나로 처리했다
  ```
  포함해야  생활인구 곡선(주차장과 무관) · 지도(별도 색으로 표시)
  제외해야  중앙값(0이 분포를 눌러 5.22 vs 6.49)
            순위표('빠듯 TOP 10'이 10개 모두 0면 동이 됨)
  ```
- 대응: 토글을 없애고 `with_parking()` 로 맥락별 처리. 판단 근거는 함수 docstring에 남김

---

### 🔍 점검했으나 문제 없던 것

**회귀 기준 시점(토·오후) 하나에만 의존하는 문제** — 실측 결과 문제 없음

기획이 "요일·시간대를 정하면 그때 여유로운 동네"를 약속하는데 회귀는 토요일 오후 하나로만
돌리고 있어 확인했다. 28개 요일×시간대를 전부 돌려 비교한 결과:

- **순위상관 0.990 ~ 1.000**, 공급부족 집합 겹침 **93% 이상**
- 즉 어느 시점을 기준으로 삼아도 추천·회피 동네가 거의 같음

오히려 새 기획을 뒷받침한다 → **"어디로 갈지는 시점과 무관하게 안정적이고,
언제 갈지는 동네마다 크게 다르다"**(가설 5 중앙값 +31%).
"왜 하필 토요일 오후냐"는 반론을 막는 근거이므로 **보고서에 견고성 검증으로 넣을 것.**

---

### 📌 남은 작업

**[보고서·발표자료] 주거지수를 정식 지표로 넣을 것**

```
주거지수 = 상주인구 ÷ (상주인구 + 직장인구)
  1에 가까움 → 주거지 (사는 사람만, 일하러 오는 사람 없음)
  0에 가까움 → 업무지구
```


| 동네        | 주거지수 | 성격                      |
| ------------- | ---------- | --------------------------- |
| 정릉3동     | 0.98     | 완전 주거지               |
| 대학동      | 0.98     | 〃                        |
| **논현2동** | **0.26** | 업무지구 (414개 중 396위) |

**왜 넣어야 하나** — 반드시 나올 반론을 막는 지표이기 때문.

> "공영주차 부족한 동네들, 그냥 주거지라서 그런 거 아닌가요? 아파트 주차장 있잖아요."

논현2동은 주거지수 0.26 · 직장인구 서울 평균 **5.62배** · 음식점 3.28배로 주거지가 아님.
그런데도 4.4만 명에 13면 → **주거지 가설로 설명 안 되는 진짜 주차난**임을 보이는 근거.

- 근거 위치: `docs/methodology.md` §5 (교란요인 검증, 주거지수 ↔ 표준화 잔차 r=-0.063 p=0.24 → 무관)
- 할 일: 위 표를 보고서 본문과 발표 슬라이드에 **정적 표/그림으로** 삽입
- 공급부족 56개 동을 주거지수로 3분류해 제시 (주거지형 / 업무·상권형 / 관광·대학형)

> **진행됨** — 주거지수는 `cluster.py`의 핵심 군집 피처로 이미 들어갔고
> `dong_cluster.csv`에 동별 값이 저장된다.
> 다만 **3분류는 데이터가 지지하지 않는다**(실루엣 기준 k=2). 위의 "3분류해 제시"는
> 2유형(상권형·주거형)으로 바꾸거나, 군집이 아니라 주거지수 3분위로 나눈 것임을 명시해야 함.

**[결정 필요] 주차요금을 추천 점수에 넣을지**

기획서 분석 변수 10개에 "평균 주차요금"이 있고 기대효과에도 요금 얘기가 있으나
**어느 스크립트에서도 쓰이지 않는다.** 사용자 프레임에서는 돈이 직접적인 선택 기준이라 넣을 값어치가 있다.

```
결측 107/424개 동 (25%)
유료 316개 / 무료 1개
중앙값 2,400원/시간 · 상위10% 4,262원 · 최대 9,000원
```

→ **결측 25%를 어떻게 처리할지 방침을 먼저 정해야 함** (무료 처리 / 중앙값 대체 / 결측 동 제외)

**[선행 조건] 관심 장소 매칭 — 카카오 Local API 403 해제**

기획서 분석 방법에 새로 넣었으나 전처리 모듈이 없다.
`fetch_poi_kakao.py`는 있으나 403(앱 서비스 미활성) 미해결이고, 장소→행정동 매칭 코드는 미작성.
**카카오 콘솔에서 앱 서비스 활성화가 선행되어야 함.** 보고서·발표자료 이후로 미루는 것을 권장.

**[Day 2 오후에 끝난 것]**

- [X]  ~~거주형태 결합~~ — `load_census.py`, 424/424 매칭
- [X]  ~~가설 6 검증~~ — `real_demand.py`, 오탐 4개 확인
- [X]  ~~나들이 적합도 지수(PCA)~~ — `suitability.py`
- [X]  ~~자치구 요약~~ — `district.py`
- [X]  ~~공간 분포 지도~~ — `maps.py`
- [X]  ~~대시보드 개편~~ — 팔레트·구조·필터·0면·지표

**[문서] 정의 불일치·드리프트 정리**

- `cluster.py`는 매력도를 **천명당 밀도**로, `recommend.py`는 **절대 개수**로 쓴다.
  각각 목적에 맞으나(성격 분류 vs 놀거리 총량) 보고서에 정의를 구분해 적을 것
- 기획서 파생변수에 주거지수가 "상주-직장인구 비율"로 뭉뚱그려져 있음 → 명시 검토
- `docs/data_sources.md` · `data_dictionary.md`가 여전히 낡음 (6종 시절·민영 서버장애 시절 서술)

**[실행 순서 주의]**

`recommend.py`는 `residual.py`의 출력(`dong_residual.csv`)을 읽는다.
→ **`residual` → `recommend`** 순서로 돌릴 것.
`cluster` · `timeslot_effect` · `explore`는 `panel.csv`만 있으면 되고,
`daily_trend`는 `data/raw/living_population/*.csv`(158MB)가 필요하다.

---

## Day 3 — 2026-08-12 (수)

### ✅ 한 작업

**문서 정합성 — 낡은 서술 대폭 수정** (`6ae45b3`)

수집 초기에 쓴 서술이 그대로 남아 실제와 어긋나 있었다. 심사에서 문서를 열었을 때
숫자가 안 맞으면 나머지 수치의 신뢰도까지 함께 떨어지므로 실측값으로 맞췄다.


| 문서                      | 낡아 있던 것                                                                                                                                                                                                                |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/data_sources.md`    | 표준데이터가**"서버 장애 / 미수집"** 인데 실제로는 주(主) 데이터 · 서울시 API가 "공급 기본"인데 실제로는 0면 동 43개 보충용 · 경계 "키 발급 대기" · 실행 순서`--days 28` · 한계에 "공영 61,035면만 확보"(실제 78,267면) |
| `docs/data_dictionary.md` | §6 컬럼명이 코드와 불일치(`parking_supply`→`parking_slots`, `adm_dong_cd`→`adm_cd`) · **존재하지 않는 `dong_summary.csv`**(PCA·군집)를 설명                                                                            |
| `data/README.md`          | 생활인구 118MB(실측**157.1MB**) · 경계 1.2MB(실측 1.0MB)                                                                                                                                                                   |

5구간 개편·총조사 결합으로 **새로 어긋난 것**도 함께 고쳤다.
`panel` 11,872행×22열(4구간) → **14,840행×31열(5구간)**, 거주형태 9컬럼 누락,
인구주택총조사 소스 설명 자체가 없었음. 예시 표 2개는 4구간 수치라 재계산했다.

> 성수2가3동이 4구간에선 *"토요일 아침(5.6)이 60% 여유"* 였는데 5구간에선 *"밤(5.9)이 67% 여유"* 다.
> **아침 추천이 바로 5구간 개편이 없애려던 실행 불가능한 답**이라, 문서가 개편 이유를 스스로 보여주게 됐다.

패널·산출물 실측으로 13개 항목을 대조해 전부 일치 확인.

**발표자료(=보고서) PPT 생성기 구축** (`1a887da` · `385f62f` · `0d2a05d` · `95b5345`)

`submission/02_발표자료/발표가이드.png`를 확인하니 **발표 10분, 초과 시 강제 종료**이고
"세부 내용보다 핵심 결과와 결론 중심"이라 명시돼 있다. 권장 시간 배분을 그대로 섹션 설계로 삼았다.

```
주제·목적·핵심질문 1분 · 데이터 2분 · 분석 4분 · 시연 2분 · 한계와 배운 점 1분
```

- `src/report/build_deck.py` — python-pptx로 **26장** 생성. 16:9, 예시 덱(SKN21 2팀) 디자인 언어
  (좌상단 파란 알약 배지 + 큰 제목, 흰 배경·여백 중심, 표는 무채색)
- `src/report/deck_figures.py` — **발표 전용 그림 8종**.
  분석용 그림은 한 장에 패널이 3~4개라 슬라이드에서 축 글자가 안 읽힌다.
  한 장에 메시지 하나, 12pt 이상, 값 라벨 직접 표기로 다시 그렸다
- 처음 계획한 25장은 장당 19초라 지킬 수 없었다 → 간지 4장 포함 26장, 내용 22장

**보고서에 EDA 절 신설** — `reports/report.md` §3.4

"EDA 했냐"는 물음에 보여줄 것은 충분한데 **이름표가 없었다.** 결과가 §4.1·§4.2에 녹아만 있었다.

- 무엇을 봤는가(5개 항목)와 산출 파일
- **이상치를 제거하지 않은 이유** — 서교동 117,158명은 오류가 아니라 실제 번화가이고 그게 분석 대상
- **EDA가 바꾼 결정 4가지** — 공휴일 제외 · 시간대 5구간 · 0면 동 보충 · 민감도 병기
- 발표자료에도 12번 슬라이드로 추가 (상관 히트맵 + 바꾼 결정)

**주제·목적·목표 명시** — 보고서 §1 앞머리 + 발표 4번 슬라이드

목표를 **숫자로 확인 가능한 형태**로 적었다. 뒤 슬라이드가 이 숫자를 하나씩 채우는 구조가 된다.
`14,840행 패널 · 가설 6개 · 추천 23 / 확충 후보 52 / 불필요 13 · 대시보드 5페이지`

### 🔧 트러블슈팅

**⑳ 로컬이 원격보다 21커밋 뒤처져 있었음 — 낡은 코드 위에서 작업**

- 세션 초반에 `PROGRESS.md`만 읽고 **git 상태를 확인하지 않은 채** 개선 작업을 진행했다
- 로컬 6커밋은 원격에 **내용까지 동일하게** 존재(리베이스된 중복, `git diff` 0줄)
- 그 사이 원격에는 15커밋이 더 있었다 — 시간대 5구간 전환, 대시보드 `pages/`→`views/` 개편,
  `report.md` 375줄 초안, 가설 2·4·5 정적 분석 스크립트
- 결과: 그날 만든 A-3(`timeslot_effect.py`)은 **원격에 이미 더 정교한 버전**(프리드먼 검정)이 있었고,
  A-2 지적은 `suitability.py`가 이미 `z_residual`을 PCA에 넣고 있어 **틀린 지적**이었다
- 조치: 작업물 백업 후 `git reset --hard origin/main`, 유효했던 문서 수정분만 재적용
- **교훈: 세션 시작 시 `git fetch && git status` 부터.** 일지 파일은 최신 상태를 보장하지 않는다

**㉑ VS Code 뷰어에서 PPT 글자가 겹쳐 보임 — 파일은 정상이었으나 규격 의존이 문제**

- 증상: Muty PPT Viewer에서 표지의 "서울 공영주차"와 "수급 진단"이 **같은 줄에 겹쳐** 출력
- PowerPoint COM으로 PDF 변환해 확인하니 **정상** → 뷰어 렌더링 한계
- 다만 파일에 뷰어 의존적인 구조가 실제로 두 가지 있었다
  1. 줄간격을 배수로 기록 (`<a:spcPct val="105000">`). PowerPoint는 읽지만 무시하는 뷰어가 있다
     → **절대값(`<a:spcPts>`)으로 변환**. 폰트 크기 × 배수를 포인트로 계산해 넣는다
  2. run 안의 개행이 `<a:br/>`가 아니라 `<a:t>` 안의 **리터럴 ``** 으로 저장됨
     (python-pptx가 그렇게 쓴다. PowerPoint는 줄로 그려주지만 규격 보장이 아니다)
     → **문단 분리로 변환**
- 표지 큰 제목은 줄마다 별도 상자로 두어 좌표로 고정
- **PDF를 함께 산출**해 뷰어 차이가 없는 제출물을 확보

**㉓ 발표 그림의 R²가 그린 데이터와 다른 집단의 값이었음**

- 핵심 결과 슬라이드에 정주수요 R²를 **0.004로 적어두었는데 그림은 0.018짜리 표본**을 그리고 있었다
- 원인: `real_demand.py`는 **비아파트 100가구 이상**(확충 검토대상)만으로 회귀하는데
  (1~7가구짜리를 넣으면 실수요 비교라는 의미가 흐려지므로) 내가 '비아파트 > 0' 전체를 그렸다
- 조치: 표본을 `real_demand.py`와 동일하게 맞추고, **R²·상관계수를 상수로 두지 않고
  그린 데이터에서 직접 계산**하게 바꿈 → 0.068 / 0.004 로 보고서와 일치. 그림에 `n` 표기 추가
- 같은 방식으로 가설 1·3 막대의 상관계수 3개도 계산식으로 전환
- **교훈: 그림에 적는 숫자는 그 그림이 그린 데이터에서 나와야 한다.** 손으로 옮기면 표본이 어긋난다

### 📌 남은 작업

**✅ 해결 — 발표자료 숫자 검증 스크립트** `src/report/verify_deck.py`

`deck_figures.py`가 그리는 **그림**은 매번 데이터를 읽어 계산하지만,
`build_deck.py`의 **표와 문장**은 손으로 적은 문자열이라 데이터가 바뀌어도 따라오지 않았다.
㉓이 정확히 그래서 생긴 사고였고, 사람이 대조해서 겨우 찾았지 구조가 막아준 게 아니었다.

표를 전부 자동 생성하는 방법도 있었으나 **서술형 표가 절반**이라 이득이 크지 않았다.
대신 **적힌 값과 계산한 값을 대조**하는 쪽을 택했다. 코드 복잡도는 그대로 두고 사고만 막는다.

```
앞으로 쓰는 법

분석을 다시 돌린 뒤에는 이 순서로 하시면 됩니다.

python -m src.analysis.residual   # ... 나머지 분석
python -m src.report.deck_figures
python -m src.report.build_deck
python -m src.report.verify_deck   # ← 여기서 걸리면 build_deck.py 수정
python -m src.report.verify_deck --strict   # 문구를 못 찾은 경우도 실패로
```

동작 — ① 생성된 pptx에서 도형·표의 모든 텍스트를 뽑고
② 정규식으로 **슬라이드에 적힌 값**을 찾고 ③ 같은 값을 `data/processed/`에서 **직접 계산**해 비교


| 결과   | 뜻                                                       |
| -------- | ---------------------------------------------------------- |
| 일치   | 슬라이드와 데이터가 같다                                 |
| 경고   | 문구를 못 찾음 — 표현을 바꿨거나 슬라이드에서 뺐다      |
| 불일치 | **데이터가 바뀌었는데 슬라이드가 안 따라왔다** → exit 1 |

검사 항목 **22개** — 패널 규모 · 공급(78,267면/358동/0면 66) · 등급(추천 23·혼잡 21·확충후보 52·불필요 13)
· 군집(112/239) · 시간대 효과(85.2%·소공동 989%·을지로동 379%) · 개별 동 수치 · 상관계수

현재 **일치 22 · 경고 0 · 불일치 0**.
추천 동네 수를 일부러 23→27로 바꿔 넣어 `[불일치] 슬라이드 27 vs 데이터 23`으로 잡히고
exit 1이 나오는 것까지 확인했다.

> 회귀 수치(R²·상관)는 그림이 직접 계산하므로 검사 대신 **값을 로그로 찍어** 눈으로 보게 했다.

**남은 것** — 발표자료 시간 압박

**시간 압박** — 26장 중 간지 4장을 빼면 내용 22장, 장당 약 22초다.
리허설에서 넘치면 ① 목차 생략 ② 간지 2~3초씩 ③ EDA 15초로 줄이는 순서를 권한다.
반대로 시간을 더 써야 할 곳은 **14번 핵심 결과**와 **19번 가설 6(아파트 오탐)** 이다.

---

## Day 4 — 2026-08-13 (목)

### ✅ 한 작업

### 🔧 트러블슈팅

### 📌 남은 작업
