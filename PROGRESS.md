# 프로젝트 진행 일지

서울시 생활인구 기반 행정동별 주차수급 불균형 진단 및 취약지역 도출 (1주 프로젝트)

기간: 2026-08-10 (월) ~ 2026-08-16 (일)

---

## Day 1 - 2026-08-10 (월)

### ✅ 오늘 한 작업

**1. 프로젝트 기획 구체화**
- 문제 정의: 주말 나들이 시 주차 만차로 시간 낭비 → **코스 결정 전에 주차 여유·혼잡도를 미리 보는 도구**
- 분석 단위 확정: **행정동 × 요일 × 시간대 = 1행** (기존 행정동 단독 → 패널 구조로 변경)
- 시간대 4구간 확정: 아침(06-11) / 점심(11-14) / 오후(14-18) / 저녁밤(18-24)

**2. 프로젝트 구조 세팅 + git 초기화**
- 디렉토리: `config`, `data/{raw,interim,processed,external}`, `src/{collect,preprocess,analysis,utils}`, `dashboard`, `reports`, `docs`, `tests`
- `git init` + 첫 커밋 (`86738b1`, 33개 파일)

**3. 수집 기반 코드 작성**
| 파일 | 역할 |
|---|---|
| `src/utils/api_client.py` | 서울 열린데이터광장 공통 클라이언트 (페이지네이션 + 재시도) |
| `src/utils/settings.py` | `.env` / `config.yaml` 로딩 |
| `src/utils/timeslot.py` | 요일·시간대 파생 (패널 키 기준) |
| `src/collect/fetch_living_population.py` | 생활인구 (행정동×시간대) |
| `src/collect/fetch_parking_lots.py` | 공영주차장 안내정보 (공급=총주차면수) |
| `src/collect/fetch_parking_realtime.py` | 시영주차장 실시간 주차대수 스냅샷 |
| `src/collect/fetch_poi_kakao.py` | 카카오 로컬 API POI 밀집도 |
| `src/collect/fetch_dong_boundary.py` | SGIS 행정동 경계 |
| `src/collect/fetch_registered_vehicles.py` | 자치구별 등록차량 |

**4. Streamlit 대시보드 스켈레톤** (홈 / 지도 / 행정동 상세)

### 🔧 트러블슈팅

**① 실시간 주차대수 컬럼 폐지 발견 (중요)**
- 문제: 공영주차장 안내정보(`GetParkInfo`, 약 14,000개)로 실시간 여유율을 만들려 했으나, `CUR_PARKING`(현재주차대수) 컬럼이 **"실시간 데이터 제공이 어려워 삭제, 추후 제공예정 없음"** 으로 폐지된 것을 확인
- 해결: **하이브리드 구조**로 설계 변경
  - 공급(총주차면수) → `GetParkInfo` 전체 사용 (모든 행정동 커버)
  - 실측 여유율 → 별도 데이터셋 **OA-21709 시영주차장 실시간 주차대수**(5분 주기) 폴링, 단 커버리지는 시영주차장으로 제한
  - 미커버 행정동 → 생활인구 기반 추정 여유율로 대체
- 남은 리스크: OA-21709의 정확한 OpenAPI 서비스명 미확인 (`config.yaml`에 `GetParkingInfo`로 임시 입력, 포털 'Open API' 탭에서 검증 필요)

**② 과거 시간대별 주차 데이터 부재**
- 문제: 공공 API는 '현재 상태'만 제공, 과거 요일×시간대 통계 없음
- 해결: 스냅샷을 **자동 스케줄 폴링**으로 일주일간 누적하기로 결정 (`data/raw/parking_snapshots/`)

**③ 카카오 API 요청 수 과다 우려**
- 문제: 행정동(약 425개) × 카테고리 4종의 장소 목록을 전부 페이징하면 요청 수가 큼
- 해결: 목록 대신 응답의 `meta.total_count`만 사용 → **요청 1회로 밀집도 확보**, 총 약 1,700회로 축소

**④ VS Code에서 `.env`가 흐리게 표시되지 않음**
- 원인: 폴더를 연 뒤에 `git init`을 해서 VS Code git 확장이 저장소를 미감지
- 확인: `git check-ignore -v .env` → `.gitignore:2:.env` 로 정상 무시됨 (GitHub 업로드 안 됨)
- 해결: Reload Window로 저장소 재인식

### 📌 남은 작업
- [ ] API 키 발급 완료 확인 (서울 열린데이터광장 / 카카오 / SGIS)
- [ ] OA-21709 실시간 주차 서비스명 검증 → `config.yaml` 수정
- [ ] 생활인구 다일자(4주) 수집으로 확장 — **작업 중단된 지점**
- [ ] 각 API 1회 호출해 실제 응답 스키마 확인 → `docs/data_dictionary.md` 갱신
- [ ] 주차 스냅샷 자동 폴링 스케줄 등록 (하루 4회)

---

## Day 2 - 2026-08-11 (화)

> ※ 아래는 Day 1(8/10) 후반 작업. 날짜 정리 시 옮길 것.

### ✅ 한 작업
- 기획서(`분석_아이디어_기획서_문지영.docx`) 작성 및 **사실 검증 후 11곳 수정** (원본은 `_원본백업.docx`)
- API 키 3종 확보: 서울 열린데이터광장 / 카카오 / 공공데이터포털
- **서울 OpenAPI 실호출 검증** — 생활인구·공영주차장 정상 작동 확인
- 수집 코드를 기획서 기준으로 재정렬
  - 신규: `fetch_parking_standard.py`(민영), `load_commercial.py`(상권 CSV)
  - 개명: `fetch_parking_lots.py` → `fetch_parking_seoul.py`
  - 삭제: `fetch_parking_realtime.py`, `fetch_registered_vehicles.py` (기획서에서 제외됨)
  - `fetch_living_population.py` 다일자(4주) 수집으로 확장
- `config.yaml` 전면 개편 (회귀 기준시점, 잔차 임계값, 상권 CSV 경로 추가)

### 🔧 트러블슈팅

**① 공공데이터포털 다운로드 실패 — 원인 규명 (서버 장애 확정)**
- 증상: "다운로드에 실패하였습니다. 잠시후 시도해 주세요."
- 분석: `std-download-manager.js`를 열어보니 파일을 직접 주는 게 아니라 **브라우저가 1만 행씩 나눠 받아 조립**하는 구조
- 실측:
  - 파일 경로 `/download/columList.json?pk=15012896` → **HTTP 500**
  - 오픈API `tn_pubr_prkplce_info_api` → **HTTP 504** (60초 후)
- 세션 쿠키·`publicDataPk`·`Referer` 정상 구성 후에도 동일 → **포털 서버 문제 확정** (브라우저·네트워크 무관)
- 대응: 서울 공영주차장으로 선진행, `--probe` 옵션으로 복구 확인 후 민영 추가

**② 서울 공영주차장에 민영이 없음을 실측 확인**
- 전량(2,189건) 수집 후 운영구분 집계 → 시간제/거주자/버스전용 등 **전부 공영, 민영 0건** (총 61,035면)
- → 민영은 공공데이터포털이 유일 경로임이 확정됨

**③ SGIS 필요 여부 판단을 번복**
- 최초: "서울 열린데이터광장에 경계가 있으니 SGIS 불필요"로 판단
- 검증: 생활인구 페이지에 GeoJSON **0건**, 발견된 파일은 **`통계지역경계(2016년 기준).zip`** (게다가 다운로드 코드가 주석 처리됨)
- 2016년 경계 사용 시 행정동 통폐합으로 조인 결측 발생 → **SGIS(최신 연도 지정 가능)로 확정**, 기획서도 수정

**④ docx 수정 시 텍스트가 여러 run으로 분할되어 치환 불가**
- 원인: Word가 문단 텍스트를 여러 `<w:r>`로 쪼개 저장
- 해결: XML 직접 편집 대신 **Word COM 자동화**로 서식 유지한 채 치환
- 추가 이슈: 활용데이터 목록이 **중첩 표**여서 `doc.Tables`로 안 잡힘 → 셀 내부 `cell.Tables` 재귀 탐색으로 해결

**⑤ Python 미설치 확인**
- `python.exe`가 Microsoft Store 스텁(버전 0.0.0.0)만 존재 → 실행 시 스토어만 열림
- python.org 정식 설치 필요 ("Add python.exe to PATH" 체크)

### 📌 남은 작업
- [ ] **Python 설치** (최우선 — 없으면 아무것도 실행 불가)
- [ ] SGIS 인증키 발급 → `.env` 입력
- [ ] 상권분석 CSV 4종 수동 다운로드 (`-행정동` 버전, OA-22xxx)
- [ ] 집객시설·상주인구 행정동 데이터셋 ID 확인
- [ ] 공공데이터포털 복구 확인 후 민영주차장 수집
- [ ] 전처리: 공간조인 + 패널 테이블 생성

---

## Day 3 - 2026-08-12 (수)

### ✅ 오늘 한 작업
-

### 🔧 트러블슈팅
-

### 📌 남은 작업
-

---

## Day 4 - 2026-08-13 (목)

### ✅ 오늘 한 작업
-

### 🔧 트러블슈팅
-

### 📌 남은 작업
-

---

## Day 5 - 2026-08-14 (금)

### ✅ 오늘 한 작업
-

### 🔧 트러블슈팅
-

### 📌 남은 작업
-

---

## Day 6 - 2026-08-15 (토)

### ✅ 오늘 한 작업
-

### 🔧 트러블슈팅
-

### 📌 남은 작업
-

---

## Day 7 - 2026-08-16 (일)

### ✅ 오늘 한 작업
-

### 🔧 트러블슈팅
-

### 📌 남은 작업
-
