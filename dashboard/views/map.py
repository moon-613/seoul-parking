"""지도 페이지 (나들이) — 행정동 경계에 선택 조건의 값을 색으로 표시.

나들이객이 볼 지표 5개만 둔다. 확충 진단용 지표는 주차 정책 그룹의
map_policy.py로 갈랐다 — 사이드바가 이미 두 사용자로 나뉘어 있는데
지도 한 장에 둘이 섞여 있으면 누구를 위한 화면인지 드러나지 않는다.

정책 지표를 덜어내면 이 페이지의 지표는 3개가 시간에 따라 변하므로
요일·시간대와 '이 시간대의 편차'가 모두 제 역할을 한다.
"""
from map_render import TRIP_METRICS, render_map

render_map(
    TRIP_METRICS,
    title="🗺️ 행정동 지도",
    lead="어디가 여유롭고 어디가 붐비는지 봅니다. "
         "선택한 요일·시간대 기준으로 색이 바뀌고, 마우스를 올리면 상세가 보입니다.",
)
