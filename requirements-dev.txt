# 로컬 전체 개발 환경 (수집·전처리·분석·보고서·테스트 포함)
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dev.txt

-r requirements.txt

# --- 데이터 수집 ---
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
tenacity>=8.2          # API 재시도

# --- 전처리 / 분석 ---
geopandas>=0.14
shapely>=2.0

# --- 시각화 ---
folium>=0.16
streamlit-folium>=0.21
matplotlib>=3.8

# --- 보고서 ---
jinja2>=3.1

# --- 개발 / 테스트 ---
pytest>=8.0
jupyter>=1.0
