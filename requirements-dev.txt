# 로컬 전체 개발 환경 (수집·전처리·분석·보고서·테스트 포함)
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dev.txt

-r requirements.txt

# --- 데이터 수집 ---
requests>=2.31
tenacity>=8.2          # API 재시도

# --- 전처리 / 분석 ---
geopandas>=0.14
shapely>=2.0
pyproj>=3.6            # SGIS 경계 EPSG:5179 -> 4326 재투영
scipy>=1.11            # 회귀(linregress) · 프리드먼 검정 — 7개 모듈이 직접 임포트

# --- 시각화 ---
matplotlib>=3.8        # 보고서용 정적 그림 (대시보드는 plotly)

# --- 보고서 / 제출물 ---
python-docx>=1.1        # 기획서 docx 편집 (Windows의 Word COM 대체)
python-pptx>=1.0        # 발표자료(=보고서) PPT 생성

# --- 개발 / 테스트 ---
pytest>=8.0
jupyter>=1.0
